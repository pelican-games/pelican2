# WP174 負債 TEST0 — テスト/台帳の保守性 完了レポート

日付: 2026-07-18
対象ブランチ: `agent/wp174-test0`

## 1. 結論

WP174 の3項目を完了した。203,012 byte の単一 golden test source/target は、
共通 static helper library と意味別4 targetへ分離した。既存16 CTest名・tag・
assertion・golden byteは変更していない。実装台帳は、WP172だけを残す active ledger と、
完了済み168 WPを逐語保存する archive に分離した。`rpc_scene_flow_headless_player` は
二回比較前に `memory.heaps[].usage/budget` だけをJSON構造から除くようにし、
driver実測値の揺れと論理的な応答差を区別する。

## 2. Golden harness 分割

### 分割前後の対応

| 分割前 | 分割後 | 所有する既存ケース | 件数 |
|---|---|---|---:|
| `pelican_test_golden_image_test` / `golden_image_test.cpp` の共通 setup・比較・readback・fixture生成 | `pelican_golden_harness` static library / `golden_harness.{hpp,cpp}` | 4 targetの全ケース本体。STB実装、Vulkan setup、PNG比較、renderer/readback helperを一箇所で所有 | 16 |
| 同 target の画像baseline群 | `pelican_test_golden_cases_test` | `golden image cases match expected output`、`golden final RGBA8 bytes match the WP74 C1b baseline hashes` | 2 |
| 同 target のtemporal/XR群 | `pelican_test_golden_temporal_test` | projection jitter 2件、TAA決定性、logical stereo、OpenXR transition、velocity 3件 | 8 |
| 同 target のGPU timing群 | `pelican_test_golden_timing_test` | byte/identity、query ring、compute、sprite anchor | 4 |
| 同 target のframegraph群 | `pelican_test_golden_framegraph_test` | renderer trace、fullscreen rebind | 2 |

`pelican_define_test` に `GOLDEN` 属性を追加し、4 targetすべてへ従来どおり
`gpu` label と `pelican_golden_gpu` resource lockを付けた。shaderc runtimeの配置も
各 executable に適用する。CTest JSONによる登録検査は次の結果だった。

- 旧/新のケース名+tag: 16/16、差分0
- assertion行: 289/289、順序差分0
- `gpu` label / resource lock不一致: 0

## 3. Implementation ledger / archive

`docs/implementation_plan.md` は冒頭相互リンク、§0共通規則、現行WP一覧、
未完了のWP172本文、運用章§3〜§5だけを持つ。新設
`docs/implementation_archive.md` は逆向きリンク、旧WP一覧と依存関係、
完了済みWP本文を持つ。`docs/README.md` の索引もactive/archiveの二項へ更新した。

archive移動WP一覧（168節）:

> WP1〜21、WP25〜31、WP33〜35、WP37〜171、WP173〜174

HEAD時点の旧 `implementation_plan.md` から各 `### WPn:` blockを再抽出し、
active/archive上の対応blockと文字列比較した。169/169 blockが逐語一致した。
同じ検査で§0、§3〜§5、旧§1も逐語一致した。逐語参照gateの例である
WP150 §3.1(B-C1)本文もarchive内に丸ごと残っている。

## 4. `rpc_scene_flow` flaky恒久対策

### 比較規則

二回のstdoutは、従来のUUID/startup実測正規化に加え、各JSON-RPC responseを
JSONとして解析してから `result.memory.heaps[]` の `usage` と `budget` キーだけを
削除して比較する。heap配列の長さ、heap index/size/source/device_local、
`driver_available`、`driver_reason`、`schema_version`、`engine_categories` と
そのlogical値、その他のresponse構造は比較に残る。

### 正負fixture

| fixture | 注入差分 | 期待 | 結果 |
|---|---|---|---|
| `baseline` vs `measured_changed` | 同一heapの`usage`/`budget`だけ変更 | 正規化後一致（PASS） | PASS |
| `baseline` vs `structure_changed` | 第2heapを追加 | 正規化後も不一致（本番比較ならFAIL） | PASS |
| `baseline` vs `driver_changed` | `driver_available`を変更 | 正規化後も不一致（本番比較ならFAIL） | PASS |
| `baseline` vs `logical_changed` | `engine_categories[].logical_used_bytes`を変更 | 正規化後も不一致（本番比較ならFAIL） | PASS |

実playerを二回起動する `rpc_scene_flow_headless_player` と、上記注入を行う
`rpc_scene_flow_normalization_fixtures` のverbose再実行は2/2 PASSだった。

## 5. 検証結果

使用したconfigure:

```text
cmake -S . -B build -DSKIP_DEVSTUDIO=ON -DPython3_EXECUTABLE=C:\Users\enjoy\AppData\Roaming\uv\python\cpython-3.12.13-windows-x86_64-none\python.exe
```

| Gate | 結果 |
|---|---|
| fresh configure | PASS |
| Debug full build | PASS（全target） |
| 全CTest | **666/666 PASS**、failure 0、485.13秒。既存allowlist対象のWindows symlink SKIP 1件のみ |
| RPC正負証明 | 2/2 PASS（実player二回比較1 + 注入fixture1） |
| golden verbose | **16/16 PASS**、SKIP 0、214.1秒。画像比較238 assertions、最終RGBA8 hash 235 assertions |
| golden inventory | inventory + negative fixtures **2/2 PASS**（negative 7件） |
| golden byte | `test/golden` 149 files、差分0。集約SHA-256 `229ab36e8ac51ebe4236b0c156c51ad2fe0dc990e51b1ec2551b905ec1cac809` |
| inventory byte | git blob `8e9a5cc74eb8f9f9fb74b274b9710f8d934c45b6`（着手前後同一） |
| player 8秒 | `projects/sprite_demo --xr off`をhidden起動。8,291 ms時点で生存、stderr 0 byte、停止後残留0 |

## 6. 変更境界

変更はgolden test構造/CMake、implementation ledger/archive/索引、
`rpc_scene_flow`比較helperとfixture、本レポートに限定した。`src/`、`.github/`、
`communication/rpcserver`本体、WP172のpreview新設面、golden asset/inventoryには
変更がない。
