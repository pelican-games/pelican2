# WP99 完了レポート — アニメ A1.5 敵対 fixture

日付: 2026-07-12

ブランチ: `agent/wp99-anim-a15`

## 結果

WP99 が指定する敵対 fixture 8 系統を `animation_jobs_test.cpp` に追加し、223 assertions
すべてが成功した。fixture は実装を前提に期待値を作らず、`implementation_plan.md` の
WP99 節、`design_animation_graph.md` v2.1、凍結済み `animation_abi_v1.md` の規範から
期待値を導いた。各 test case に根拠となる仕様文をコメント引用している。

敵対 fixture により実バグを 2 件発見し、凍結 public ABI と golden を変更せず最小修正した。
仕様の曖昧点・矛盾・未解決質問はない。

## fixture と仕様引用の対応

| # | fixture | 仕様引用と期待値 | 結果 |
|---|---|---|---|
| 1 | non-joint 祖先 | `design_animation_graph.md` §1-1「全アニメ階層（skin joint 集合ではない — non-joint ノードを含む）」。Z 90 度回転を持つ非 joint 中間 node の子 `(1,0,0)` は palette で `(0,1,0)` になる。joint-only 縮約では得られない値 | PASS |
| 2 | 同骨数別 rig | `animation_abi_v1.md` §2「同じ node 数、同じ名前集合、同じ byte size は互換性の根拠にならない」。同数だが別名・別 identity の Pose を blend すると `incompatible_layout`、出力は不変 | PASS |
| 3 | loop 跨ぎ event/root | 同 §4.2「0/1/N loop を unwrapped time 上で列挙」「同時刻は `(source, ordinal)` 昇順」「seek は crossing を emit せず root delta を identity」。forward 2.5 周、reverse 2.2 周、seek 直後の crossing、loop index、signed root delta を固定 | PASS |
| 4 | N-way 順序不変 | `design_animation_graph.md` §6「同一 build/architecture での byte 一致」と ABI §6 の sign canonicalize・zero weight 規範。4 source（正 weight 3、zero weight 1）の全 24 permutation で T/R/S byte 一致 | PASS（バグ発見） |
| 5 | hot reload stale handle | ABI §2〜4「解放前に generation を進める」「旧 handle は `stale_generation`」「失敗時は部分更新しない」。registry clear 後の Rig/Clip、frame reset 後の Pose、destroy 後の Cursor を検査し、全 out sentinel 不変 | PASS（バグ発見） |
| 6 | 2 体並列 | ABI §3「並列 actor 評価は thread ごとに独立 arena」、§9「instance identity へ publish」。同一 rig を 2 thread で異なる時刻に評価し、arena address、palette、instance revision が非混線 | PASS |
| 7 | clamp 端再 emit 禁止 | ABI §4.2「端到達後に同じ annotation を再 emit しない」。forward/reverse の各端で初回だけ emit、端以後は crossing 0・root delta 0 | PASS |
| 8 | capacity 境界 | ABI §4.1「capacity 超過は `buffer_too_small`」「配列は書かず cursor を進めない」。capacity 0 query、1 少ない buffer、required と同数の fill を同一 cursor で順に検査 | PASS |

## 発見バグと最小修正

### 1. Rig/Clip と Pose の stale-generation enforcement が不足

変更前の `AnimationAssetRegistry::clear()` は asset map を消して identity counter を進めるだけで、
既に発行した Rig/Clip handle の generation を失効させる記録がなかった。そのため jobs API は
旧 handle を `stale_generation` と判定できず、ABI §2 の「unload/reload は参照メモリ解放前に
generation を進める」に違反していた。

また jobs 側の PoseView 検査は handle の非 zero と layout identity だけを確認していたため、同じ
layout で arena frame reset 済みの PoseView を `samplePoseAt` / `blendNormal` / `localToModel` が
受理できた。これは ABI §3 の「reset 後に engine API へ渡すと `stale_generation`」に違反していた。

修正は asset 内部で共有する generation token と、engine 内部の Pose slot registry の追加に限定した。
`clear()` は map erase 前に
token を進め、`samplePoseAt`、`blendNormal`、`localToModel`、`buildSkinPalette` は出力へ触れる前に
token と handle generation を検査する。Pose identity は arena の acquire slot ごとに安定させ、
`beginFrame` で generation を進めて旧 slot handle を stale にし、jobs の PoseView 検査からも同じ
registry を参照する。registry の active slot 数は arena の最大同時 acquire 数に有界である。
失敗時の caller-owned Pose、matrix、palette は不変である。
これは `animationjobs.*` の engine 内部型だけの変更であり、凍結 ABI header の変更ではない。

### 2. N-way quaternion が source permutation により signed zero を漏らす

凍結済み hemisphere reference algorithm は正しく数値を返していたが、reference による符号反転で
ある permutation が `+0.0`、別 permutation が `-0.0` を返した。数値比較では同値でも byte は異なり、
WP99 #4 と設計 §6 の byte 決定性に違反した。fixture では quaternion の非 zero 成分は完全一致し、
`x/y` の zero sign bit だけで `memcmp` が失敗することを確認した。

修正は frozen sign canonicalization 完了後、各成分の zero を `+0.0` へ正規化する処理だけである。
hemisphere reference、descriptor 配列順 double accumulate、normalize、最終非 zero 符号規則は変更していない。

## ABI・golden

- `src/core/userpublic/animation/abi_v1.hpp`: byte 無変更
- `docs/animation_abi_v1.md`: 無変更
- descriptor、handle、enum、function table: 変更なし
- `test/golden/`、expected PNG、tolerance、case JSON: 変更なし
- golden の再基準化・追加: なし

## 検証

指定された Python 3.12.13 と `SKIP_DEVSTUDIO=ON` を使用した。

```text
cmake -S . -B build-wp99 -DSKIP_DEVSTUDIO=ON \
  -DPython3_EXECUTABLE=C:\Users\enjoy\AppData\Roaming\uv\python\cpython-3.12.13-windows-x86_64-none\python.exe
cmake --build build-wp99 --config Debug --parallel 2
ctest --test-dir build-wp99 -C Debug --output-on-failure -j 4
```

- configure: PASS（Visual Studio 17 2022）
- Debug 全 target build: PASS
- WP99 focused: **8 test cases / 223 assertions PASS**
- 全 CTest: **328/328 PASS、failure 0**
  - Windows symlink 作成権限依存の既存 PathResolver 1 case のみ conditional Skip
  - Animation / ABI / WP99 / golden の Skip は 0
- golden verbose: **24/24 cases、81 assertions PASS、SKIP 0**
- `golden final RGBA8 bytes match the WP74 C1b baseline hashes`: 既存 24 hash + 件数検査、
  **25 assertions PASS**
- `git diff --check`: PASS

## player 8 秒起動

tracked `projects/example` は既存レポート記載どおり未同梱の外部 model asset を参照するため、
`pelican_cli project init` が生成した 16-file の自己完結 project を使用した。通常 windowed player は
8 秒後も main loop 生存、stderr 0 byte、fatal / validation error 0 件だった。確認後は PID 指定で停止し、
残留 `pelican_player` process が 0 件であることを確認した。

## 逸脱・質問

逸脱なし。仕様の追加解釈や ABI 変更を要する質問はない。
