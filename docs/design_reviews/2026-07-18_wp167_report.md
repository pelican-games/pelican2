# WP167 BEH2 完了報告

## 1. 結論

WP167「BEH2 — behavior の編集統合 (attach/remove/set-param)」を完了した。
behavior attachment は component 名だけでなく attachment index/handle を安定 target として
E-RPC transaction と journal に参加し、WP155 の lifecycle barrier、WP158 の behavior
junction、WP164 の schema 駆動 Inspector を接続した。

G2 の同時発生規範は **reload/callback/reconcile 中の behavior 編集を安定 reject** とした。
Debug 全ビルド、全 643 CTest、golden 8 件、実機 player/Inspector 編集を通過しており、
未解決事項はない。

## 2. `design_object_behaviors.md` v2.1 §6 逐語との 1:1 対応

> behavior attach/remove/set-param は generic ECS component name だけでなく attachment
> handle/index を target にし、E-RPC transaction/journal へ参加する。attach は commit 後の
> 次 activation boundary で onInit、remove は commit 前 pre-destroy で onDestroy、params
> edit は「live instance へ atomic apply」を固定し、同一 frame の onEvent/onUpdate から
> 見える版を規範化する。undo/redo、replay/golden reject、G2 同時発生、type combo の owner
> generation 更新、pending params 表示を fixture にする。

| §6 の逐語条件 | 実装 | fixture / 確認結果 |
|---|---|---|
| `attachment handle/index を target` | journal operation、stable target、write domain に `attachment_index`、`attachment_handle`、`attachment_seq` を保持する。accept 時に runtime identity を一度だけ予約し、undo/redo でも同じ identity を使う。 | `WP167 direct behavior attachment edits keep exact handles and sequences`。同型 attachment 2 個を個別に set/remove できる。 |
| `E-RPC transaction/journal へ参加` | `add_component` / `remove_component` の `type="behavior"` と `set_component_value` を journal の prepare/forward/inverse と projection execution に実装した。WP158 `makeBehaviorAttachmentProjectionJunction()` を aggregate transaction の最後の participant として消費する。 | direct attach/remove/set、undo、redo、stale target、invalid params を `editorjournal_test` で検証。 |
| `attach は commit 後の次 activation boundary で onInit` | prepare では instance と params を一時生成するだけで、publish 時は `active=false, activation_delay=1`。commit frame の event/update snapshot には入らず、次 boundary で `onInit` を 1 回実行してから callback 対象になる。 | `WP167 live behavior edits preserve identity lifecycle and temporal visibility` で同 frame 非可視、次 boundary の `onInit` と update/event 順を検証。 |
| `remove は commit 前 pre-destroy で onDestroy` | behavior junction の no-throw publish は document publish より前に実行され、live entity と params が解決可能な間に `onDestroy` を呼び、その後 arena から消す。prepare 失敗時には呼ばない。 | 同 fixture で `onDestroy` 内の entity alive、exact handle/seq、commit 後の attachment 消失を検証。 |
| `live instance へ atomic apply` | `BehaviorParamsPolicy` で temporary decode/canonical validation を完了後、登録時に要求した nothrow-swappable な `Params` と live params を no-throw swap する。behavior instance は recreate せず、handle/seq/owner を維持する。raw params 省略時は一時的に default 展開して編集し、undo では元の省略形を復元する。 | arena fixture で invalid decode の無変更、init/destroy 不発、identity 維持を検証。`WP167 params edit expands defaults temporarily and undo restores raw omission` も追加。 |
| `同一 frame の onEvent/onUpdate から見える` | set-param の swap は publication interval 内で完了するため、その後に取得する event/update callback は新 params を読む。 | arena fixture で set commit と同 frame の event/update trace が新値になることを検証。 |
| `undo/redo` / B-C3 | attach の inverse は同 seq/handle の remove、remove の inverse は同 seq/handle と raw params の attach。forward → inverse → forward で identity を再割当しない。 | direct attach/remove の各経路で seq 列と lifecycle/event/update trace の一致を検証。forward と redo の handle/seq は同一。 |
| `replay/golden reject` | behavior edit も既存 edit gate の replay/golden 理由を消費し、identity allocation や document/runtime prepare より前に reject する。 | `WP167 replay and golden gates reject behavior edits before identity allocation`。golden の `-R golden -V` にも同 fixture が入る。 |
| `G2 同時発生` | `behaviorCallbackActive()`、game-logic reload active、ReloadService `reconciling` のいずれかを `reload_scene_transition` gate 理由へ正規化し、accept 時と execute 時の双方で同じ reject にする。 | `WP167 G2 reload overlap is a stable reject at acceptance and execution`。3 状態と通常時を検証。 |
| `type combo の owner generation 更新` | runtime query は attachment ごとに owner と generation 状態を返し、reload 後の owner metadata を再取得する。set-param は name+owner の一致を prepare 時に再検証する。 | Inspector fixture で同型 attachment の owner generation 更新と handle 501/502 の個別表示を検証。既存 BEH1 reload fixture 群も全 PASS。 |
| `pending params 表示` | DLL 登録がない pending behavior は raw params を保持し、schema widget を無効化して `Pending \| game-logic DLL unavailable \| read-only` badge を表示する。 | `WP167 inspector distinguishes repeated behavior attachments and pending DLL state` で badge、read-only、raw params、schema pointer を検証。 |

## 3. activation / publication boundary

```text
frame N
  acceptance gate
    ├─ replay / golden                         -> reject
    └─ behavior callback / DLL reload / G2    -> stable reject
          |
          v
  journal prepare
    ├─ handle + attachment_seq を一度だけ確定
    ├─ authored document を stage
    └─ behavior params/instance を temporary decode + validate
          |
          v
  E-RPC aggregate transaction (observer-free publication interval)
    ├─ existing adapters publish
    ├─ behavior junction publish (最後)
    |    ├─ attach: inactive, activation_delay=1
    |    ├─ remove: onDestroy(entity は alive) -> arena erase
    |    └─ set: prepared Params を live Params と no-throw swap
    └─ document publish -> commit revision
          |
          +-- frame N の onEvent/onUpdate
              attach は不可視 / set の新 params は可視 / remove は不可視

frame N+1 activation boundary
  activation_delay=0 の attach -> onInit exactly once -> onEvent/onUpdate
```

prepare 中の decode、schema、owner、stale target のどれかが失敗した場合は document と arena の
双方が未変更のまま終わる。behavior junction は aggregate の最後に置き、publish 自体を
allocation-free/no-throw にしたため、その後は既存の no-throw document publication だけが残る。

## 4. 主な変更点

- `behaviorarena` に editor edit batch の prepare/publish token、runtime identity 予約、index shift、
  delayed activation、pre-destroy remove、prepared params swap を追加した。
- behavior registerer に type-erased temporary params と prepare/apply/destroy 関数を追加し、
  `Params` の nothrow swap を登録時 static assertion にした。
- editor journal/service/runtime factory に behavior canonical validation、exact attachment target、
  stable write domain、raw inverse、identity resolver、G2 gate、runtime owner/generation query を追加した。
- Inspector は同型 attachment を index/handle 付きの一意 ID で描画し、behavior params でも
  WP164 の schema widget を再利用する。pending は read-only raw JSON と badge を描画する。
- frozen schema header、`editorprojectiontransaction`、projection adapters の本体は変更せず、
  既存 junction interface のみを利用した。

## 5. 検証結果

### Configure / build

```powershell
cmake . -B build -DSKIP_DEVSTUDIO=ON -DPython3_EXECUTABLE=C:\Users\enjoy\AppData\Roaming\uv\python\cpython-3.12.13-windows-x86_64-none\python.exe
cmake --build build --config Debug -- /m:2 /p:GenerateDebugInformation=false
```

- configure: 成功。
- Debug 全ターゲット build: 成功、exit 0、298.1 秒。
- PDB を生成しない最終リンクのため、以前生成した PDB を参照する LNK4099 warning はあるが、
  executable/DLL/test target はすべて生成されている。

### Test

```powershell
ctest --test-dir build -C Debug --output-on-failure
ctest --test-dir build -C Debug -R golden -V
```

- 全 CTest: **643/643 PASS、0 failed**、411.81 秒。
- Windows で symlink 作成権限を要求する既知の
  `PathResolver rejects symlink escapes after canonicalization` だけは CTest の明示的 Skipped。
  WP167/golden test の skip ではない。
- golden: **8/8 PASS**、117.6 秒、`SKIP_MATCHES=0`。
- `golden final RGBA8 bytes match the WP74 C1b baseline hashes` と `golden_inventory` を含めて成功し、
  `test/golden` の working-tree diff は 0。baseline byte の更新はない。
- WP167 focused fixture の内訳:
  - behavior arena: 9 cases / 125 assertions
  - editor journal: 16 cases / 535 assertions
  - Inspector: 7 cases / 172 assertions

### 実機 player / Inspector

- `pelican_player.exe` を WP155 behavior fixture DLL と behavior project で `--xr off` 起動。
- player log 開始 `19:48:06`、証跡取得 `20:04:04` で **15 分 58 秒**継続動作し、
  8 秒条件を超えて応答を維持した。
- Inspector で同型 behavior 2 個が `[index 0]` / `[index 1]` として別 widget になり、
  先頭の schema field を `wp167-live` に編集。画面上で `committed at revision 2` を確認した。
- 証跡: [2026-07-18_wp167_inspector_behavior_edit.png](2026-07-18_wp167_inspector_behavior_edit.png)
- 証跡取得後に対象 Debug player を正常終了し、この worktree の player process が残っていないことを確認した。

## 6. 排他・凍結領域

`git diff --name-only` で次の差分が **0** であることを確認した。

- schema 三 header
- `editorprojectiontransaction` / projection adapters 本体
- `.github/`
- ECS 実装
- golden baseline

WP155/162 の既存 reload/barrier/owner cleanup fixture を含む全テストも無変更で通過している。

## 7. 完了判定

§6 逐語、B-C2、B-C3、G2、replay/golden、owner generation、pending Inspector、
player 8 秒、golden byte 不変の全受け入れ条件を満たした。逸脱・未決事項・後続 blocker はない。
