# 語彙・型の重複センサス

調査スナップショット:

- ブランチ: `census/vocab`
- 起点コミット: `6287a5552325db893df168f21d3895caf17e8465`
- 対象: 2026-07-26 16:24 JST 時点のディスク上の内容
- 方法: `src/project/` と `src/core/renderingpass/` のヘッダを読み、候補型について `src/**/*.{hpp,cpp}` の全シンボル参照を検索した。ビルド、テスト、`src/` の変更は行っていない。

## 1. 同義に近い型の対

### 1.1 エンジン内部の対

| 型 A / 型 B | 情報の差 | 変換箇所 | 層 | 判断 |
|---|---|---|---|---|
| `GraphVariantViewExecution` (`src/project/graphvariantpolicy.hpp:40`)<br>`VulkanScopeViewExecution` (`src/project/vulkanviewplanning.hpp:28`) | A のみ `caller_defined`。B のみ `multiview`。共通する `single_view` / `sequential` は同名。A の XR 値は論理 graph variant の `sequential` であり (`src/project/graphvariantpolicy.cpp:474-496`)、B はノードごとの物理実行結果を表す。 | A↔B の直接変換はなし。A は `compileGraphVariantPolicy` (`src/project/graphvariantpolicy.cpp:451-497`) で決まり、B は別入力である XR preference、端点 capability、ノード eligibility から `resolveVulkanViewExecutionPlan` (`src/project/vulkanviewplanning.cpp:174-433`) が決める。 | A: logical。B: physical。 | |
| `XrViewExecutionPreference` (`src/project/xrtargetpolicy.hpp:16`)<br>`VulkanScopeViewExecution` (`src/project/vulkanviewplanning.hpp:28`) | A は要求 (`automatic` / `sequential` / `require_multiview`)。B は結果 (`single_view` / `sequential` / `multiview`)。A は view-independent node を表せず、B は `single_view` で表す。B は「必須」か「自動」かを保持しない。 | `resolveVulkanViewExecutionPlan` (`src/project/vulkanviewplanning.cpp:174-433`) の1関数に集約。要求だけでなく端点とノードの情報を使うため、値だけの1対1変換ではない。 | A: target-policy input。B: physical。 | |
| `VulkanScopeViewExecution` (`src/project/vulkanviewplanning.hpp:28`)<br>`GraphicsPipelineViewExecution` (`src/core/shader/graphicsviewcontract.hpp:8`) | A のみ `sequential`。B にない A の `sequential` と `single_view` は、ともに B の既定 `single_view` contract になる。対応する struct では `VulkanNodeViewExecutionPlan` が `node`、`execution_count`、`reason` を追加で持ち、`GraphicsPipelineViewContract` は pipeline binding に必要な `execution`、`view_count`、`view_mask` のみを持つ (`src/project/vulkanviewplanning.hpp:62-70`, `src/core/shader/graphicsviewcontract.hpp:16-39`)。 | `passViewContract` (`src/core/renderingpass/renderingpassruntimecompiler.cpp:133-158`) の1関数に集約。直接の enum cast はなく、`multiview` のときだけ `GraphicsPipelineViewContract::multiview` を作る。 | A: physical。B: runtime graphics-pipeline contract。 | |
| `GraphVariantResourceLayout` (`src/project/graphvariantpolicy.hpp:46`)<br>`VulkanResourceViewLayout` (`src/project/targetrenderplanning.hpp:169`) | A は `shared_2d` / `sequential_2d`。B はそれらに `layered_2d_array` を加える。A は graph variant 全体の論理 layout、B は resource ごとの物理 layout。 | A↔B の直接変換はなし。A は `compileGraphVariantPolicy` (`src/project/graphvariantpolicy.cpp:451-497`) で決まり、B は resource の read/write と物理 view execution から `applyResourceViewLayouts` (`src/project/targetrenderplanning.cpp:1835-1920`) が決める。 | A: logical。B: physical。 | |
| `VulkanResourceViewLayout` (`src/project/targetrenderplanning.hpp:169`)<br>`PassInputViewDimension` (`src/core/renderingpass/renderingpass.hpp:167`) | 列挙値の差はなし (`shared_2d` / `sequential_2d` / `layered_2d_array`)。A は物理 resource plan の属性、B は runtime `PassDefinition::input_target_views` の入力スロット属性。 | `inputViewDimension` (`src/core/renderingpass/renderingpassruntimecompiler.cpp:161-186`) の1関数に集約。plan がない互換経路だけ B の `shared_2d` を直接返す (`:164-165`)。 | A: physical。B: runtime。 | |
| `LogicalGraphNodeKind` (`src/project/logicalrendergraph.hpp:152`)<br>`FramePlanNodeKind` (`src/core/renderingpass/frameplanner.hpp:13`) | 列挙値の差はなし (`render` / `compute` / `anchor` / `snapshot_copy` / `output_transform`)。A は canonical logical node、B は legacy/runtime frame-graph definition と schedule で共用される。 | B→A は `logicalNodeKind` (`src/core/renderingpass/logicalframegraphadapter.cpp:15-26`) の1関数に集約。A→B の変換はない。`FrameGraphDefinition` から作る logical graph は diagnostic-only と明記される (`src/core/renderingpass/logicalframegraphadapter.cpp:404-411`)。 | A: logical。B: runtime frame planning。 | |
| `FrameGraphAttachmentAspect` (`src/core/renderingpass/frameplanner.hpp:21`)<br>`VulkanPhysicalAttachmentAspect` (`src/project/targetrenderplanning.hpp:243`) | 列挙値の差はなし (`color` / `depth`)。B の値は `node` と `logical_resource` で識別される物理 attachment plan に入る。 | `physicalAttachmentAspect` (`src/core/renderingpass/renderingsamplecount.cpp:614-624`) の1関数に集約し、`physicalAttachmentPlans` (`:652-679`) からだけ呼ぶ。 | A: runtime/config frame graph。B: physical。 | |
| `FrameGraphAttachmentLoadOp` (`src/core/renderingpass/frameplanner.hpp:26`)<br>`VulkanPhysicalAttachmentLoadOp` (`src/project/targetrenderplanning.hpp:251`) | 列挙値の差はなし (`load` / `clear` / `discard`)。runtime Vulkan では `discard` が `vk::AttachmentLoadOp::eDontCare` になる。 | A→B は `physicalAttachmentLoadOp` (`src/core/renderingpass/renderingsamplecount.cpp:626-638`) に集約。上流の `vk`→A は `frameGraphAttachmentLoadOp` (`src/core/renderingpass/frameplanner.cpp:305-318`)、B→runtime `vk` は `runtimeAttachmentLoadOp` (`src/core/renderingpass/renderingpassruntimecompiler.cpp:189-201`) にそれぞれ集約。 | A: runtime/config frame graph。B: physical。`vk` 値: runtime。 | |
| `FrameGraphAttachmentStoreOp` (`src/core/renderingpass/frameplanner.hpp:32`)<br>`VulkanPhysicalAttachmentStoreOp` (`src/project/targetrenderplanning.hpp:260`) | 列挙値の差はなし (`store` / `discard`)。runtime Vulkan では `discard` が `vk::AttachmentStoreOp::eDontCare` になる。 | A→B は `physicalAttachmentStoreOp` (`src/core/renderingpass/renderingsamplecount.cpp:640-650`) に集約。上流の `vk`→A は `frameGraphAttachmentStoreOp` (`src/core/renderingpass/frameplanner.cpp:320-330`)、B→runtime `vk` は `runtimeAttachmentStoreOp` (`src/core/renderingpass/renderingpassruntimecompiler.cpp:203-213`) にそれぞれ集約。 | A: runtime/config frame graph。B: physical。`vk` 値: runtime。 | |
| `ResolvedSampleCountResource` (`src/project/samplecountplanning.hpp:70`)<br>`RenderingSampleCountAssignment` (`src/core/renderingpass/renderingsamplecount.hpp:41`) | A のみ `format`。B は `resource` と `samples` のみで、sample-count plan にない runtime target にも既定 `1` のレコードを作る。 | `compileRenderingTargetPlans` 内に inline: A から `merged_samples` を作る箇所 (`src/core/renderingpass/renderingsamplecount.cpp:1800-1826`) と、B を作る箇所 (`:1929-1943`) に分かれる。専用変換関数はない。 | A: physical resolution result。B: runtime target assignment。 | |
| `CompiledMaterialRoute` (`src/project/renderpipeline.hpp:229`)<br>`CompiledMaterialRouteBinding` (`src/core/renderingpass/framegraphruntime.hpp:34`) | A は `pass_name`、B は解決済み `PassId` を持つ。`route` と `pass_contract` は同じ型・同じ値。B は元の pass name を保持しない。 | `bindMaterialRoutes` (`src/core/renderingpass/framegraphruntime.cpp:28-64`) の1関数に集約。pass name の一意性と contract 一致を検査してから B を作る。 | A: logical compiled pipeline。B: runtime compiled graph。 | |
| `FramePlanNode` (`src/core/renderingpass/frameplanner.hpp:100`)<br>`FrameGraphExecutionNode` (`src/core/renderingpass/framegraphruntime.hpp:27`) | A は declaration/order/level、read/write、snapshot 情報を持つ。B は実行対象配列の `index` と `incoming_barriers` を持つ。`name` と `kind` は共通。B の `index` は render pass または compute task の配列 index で、A の `order` ではない。 | `compileExecution` の node loop (`src/core/renderingpass/framegraphruntime.cpp:102-130`) と barrier loop (`:132-162`) に集約。 | A: runtime planning。B: runtime executable。 | |
| `FramePlanBarrier` (`src/core/renderingpass/frameplanner.hpp:113`)<br>`CompiledFrameGraphBarrier` (`src/core/renderingpass/framegraphruntime.hpp:20`) | A は string の `kind` / `from` / `to`。B は `from_node_index` と両端の `FramePlanNodeKind` を持ち、barrier kind は保持しない。現実装が受理する A の kind は `read_after_write` のみ。 | `compileExecution` の barrier loop (`src/core/renderingpass/framegraphruntime.cpp:132-162`) に集約。 | A: runtime planning。B: runtime executable。 | |
| `MaterialPassContract` (`src/project/renderpipeline.hpp:46`)<br>`MaterialRouteClass` (`src/project/renderpipeline.hpp:25`) | A は route と attachment/shader ABI の組を表す。A の `legacy_gbuffer_v1` には B の route がなく、`materialPassRoute` は `nullopt` を返す。B は route のみ。 | B→A は `expectedContract` (`src/project/renderpipeline.cpp:63-73`)。A→B は `materialPassRoute` (`:517-528`)。いずれも同一 `.cpp` の関数に集約。 | 両方 logical material-routing contract。 | |
| `MaterialPassContract` (`src/project/renderpipeline.hpp:46`)<br>`MaterialShaderContract` (`src/project/renderpipeline.hpp:32`) | A は route/phase も区別する。B は fragment-output ABI のみで、A の forward opaque/transparent は同じ B の `forward_scene_color_v1` になる。 | A→B は `materialPassShaderContract` (`src/project/renderpipeline.cpp:530-540`) の1関数に集約。B→A は一意でないため変換なし。 | 両方 logical material ABI。 | |
| `VulkanPhysicalResourceFormatCapability` (`src/project/targetrenderplanning.hpp:391`)<br>`RenderingImageFormatCapability` (`src/core/renderingpass/renderingsamplecount.hpp:24`) | A は `logical_resource` と文字列 `format` を持つ。B は対象を引数側で受け、A にない `transient_attachment_supported` と `local_read_attachment_supported` を持つ。A には共通4フィールドだけが写る。 | B→A は `physicalFormatCapabilities` (`src/core/renderingpass/renderingsamplecount.cpp:923-972`) の1関数に集約。B の取得は `queryFormatCapability` (`:709-744`)。 | A: physical-fragment validation evidence。B: runtime/device fact query result。 | |

### 1.2 公開 ABI に写す対

この表の ABI 型は engine type と同じ列挙集合を別 namespace、固定 `std::uint32_t`、明示数値で表す。変換はすべて engine→ABI の一方向である。

| 型 A / 型 B | 情報の差 | 変換箇所 | 層 | 判断 |
|---|---|---|---|---|
| `RenderPipelineGraphVariant` (`src/project/graphvariantpolicy.hpp:12`)<br>`RenderStrategy::GraphVariantV1` (`src/core/userpublic/render/render_strategy_abi_v1.hpp:72`) | 列挙集合の差はなし。B のみ ABI underlying type と数値を固定。 | `graphVariant` (`src/core/renderingpass/renderstrategyregistry.cpp:274-286`) に集約。 | A: logical。B: logical contract の provider ABI 表現。 | |
| `GraphVariantHistoryPolicy` (`src/project/graphvariantpolicy.hpp:21`)<br>`RenderStrategy::HistoryPolicyV1` (`src/core/userpublic/render/render_strategy_abi_v1.hpp:78`) | 列挙集合の差はなし。B のみ ABI 数値を固定。 | `historyPolicy` (`src/core/renderingpass/renderstrategyregistry.cpp:288-298`) に集約。 | A: logical。B: provider ABI。 | |
| `GraphVariantProjectionJitterPolicy` (`src/project/graphvariantpolicy.hpp:26`)<br>`RenderStrategy::ProjectionJitterPolicyV1` (`src/core/userpublic/render/render_strategy_abi_v1.hpp:83`) | 列挙集合の差はなし。B のみ ABI 数値を固定。 | `projectionJitterPolicy` (`src/core/renderingpass/renderstrategyregistry.cpp:300-314`) に集約。 | A: logical。B: provider ABI。 | |
| `GraphVariantViewFamily` (`src/project/graphvariantpolicy.hpp:31`)<br>`RenderStrategy::ViewFamilyV1` (`src/core/userpublic/render/render_strategy_abi_v1.hpp:88`) | 列挙集合の差はなし。B のみ ABI 数値を固定。 | `viewFamily` (`src/core/renderingpass/renderstrategyregistry.cpp:316-328`) に集約。 | A: logical。B: provider ABI。 | |
| `GraphVariantViewExecution` (`src/project/graphvariantpolicy.hpp:40`)<br>`RenderStrategy::ViewExecutionV1` (`src/core/userpublic/render/render_strategy_abi_v1.hpp:94`) | 列挙集合の差はなし。B のみ ABI 数値を固定。 | `viewExecution` (`src/core/renderingpass/renderstrategyregistry.cpp:330-343`) に集約。 | A: logical。B: provider ABI。 | |
| `GraphVariantResourceLayout` (`src/project/graphvariantpolicy.hpp:46`)<br>`RenderStrategy::ResourceLayoutV1` (`src/core/userpublic/render/render_strategy_abi_v1.hpp:100`) | 列挙集合の差はなし。B のみ ABI 数値を固定。 | `resourceLayout` (`src/core/renderingpass/renderstrategyregistry.cpp:345-355`) に集約。 | A: logical。B: provider ABI。 | |
| `GraphVariantTerminal` (`src/project/graphvariantpolicy.hpp:51`)<br>`RenderStrategy::TerminalV1` (`src/core/userpublic/render/render_strategy_abi_v1.hpp:105`) | 列挙集合の差はなし。B のみ ABI 数値を固定。 | `terminal` (`src/core/renderingpass/renderstrategyregistry.cpp:357-370`) に集約。 | A: logical。B: provider ABI。 | |
| `GraphVariantMirrorOutput` (`src/project/graphvariantpolicy.hpp:57`)<br>`RenderStrategy::MirrorOutputV1` (`src/core/userpublic/render/render_strategy_abi_v1.hpp:111`) | 列挙集合の差はなし。B のみ ABI 数値を固定。 | `mirrorOutput` (`src/core/renderingpass/renderstrategyregistry.cpp:372-382`) に集約。 | A: logical。B: provider ABI。 | |
| `CompiledGraphVariantPolicy` (`src/project/graphvariantpolicy.hpp:89`)<br>`RenderStrategy::GraphVariantPolicyV1` (`src/core/userpublic/render/render_strategy_abi_v1.hpp:116`) | A のみ `rendering_pass_name_suffix`。B のみ `struct_size`、`version`、reserved fields。残る9個の意味フィールドは対応する。 | `makeGraphVariantPolicy` (`src/core/renderingpass/renderstrategyregistry.cpp:384-404`) に集約し、上記8変換を呼ぶ。 | A: logical compiled policy。B: provider ABI envelope。 | |
| `LogicalPortDirection` (`src/project/logicalrendergraph.hpp:17`)<br>`RenderPass::PortDirectionV1` (`src/core/userpublic/render/pass_implementation_abi_v1.hpp:52`) | 列挙集合の差はなし。A は `std::uint8_t`、B は固定 ABI `std::uint32_t`。 | `portDirection` (`src/core/renderingpass/passimplementationregistry.cpp:110-122`) に集約。 | A: logical。B: pass-provider ABI。 | |
| `LogicalAccessMode` (`src/project/logicalrendergraph.hpp:95`)<br>`RenderPass::AccessModeV1` (`src/core/userpublic/render/pass_implementation_abi_v1.hpp:58`) | 列挙集合の差はなし。underlying type のみ異なる。 | `accessMode` (`src/core/renderingpass/passimplementationregistry.cpp:124-136`) に集約。 | A: logical。B: pass-provider ABI。 | |
| `LogicalAccessIntent` (`src/project/logicalrendergraph.hpp:103`)<br>`RenderPass::AccessIntentV1` (`src/core/userpublic/render/pass_implementation_abi_v1.hpp:64`) | 列挙集合の差はなし。underlying type のみ異なる。 | `accessIntent` (`src/core/renderingpass/passimplementationregistry.cpp:138-156`) に集約。 | A: logical。B: pass-provider ABI。 | |
| `LogicalReadFootprintKind` (`src/project/logicalrendergraph.hpp:114`)<br>`RenderPass::ReadFootprintV1` (`src/core/userpublic/render/pass_implementation_abi_v1.hpp:73`) | 列挙集合の差はなし。underlying type のみ異なる。radius はどちらの enum にも含まず、周辺 struct の別フィールドで運ぶ。 | `readFootprint` (`src/core/renderingpass/passimplementationregistry.cpp:158-174`) に集約。 | A: logical。B: pass-provider ABI。 | |
| `LogicalMaterializationRequirement` (`src/project/logicalrendergraph.hpp:49`)<br>`RenderGraphTransform::MaterializationV1` (`src/core/userpublic/render/graph_transform_abi_v1.hpp:64`) | 列挙集合の差はなし。A は `std::uint8_t`、B は固定 ABI `std::uint32_t`。 | `materialization` (`src/core/renderingpass/graphtransformregistry.cpp:219-240`) に集約。 | A: logical。B: graph-transform ABI。 | |
| `LogicalMaterializationRequirement` (`src/project/logicalrendergraph.hpp:49`)<br>`RenderSubgraph::MaterializationV1` (`src/core/userpublic/render/subgraph_replacement_abi_v1.hpp:59`) | 列挙集合の差はなし。上の ABI 型とは namespace だけでなく別定義。 | `materialization` (`src/core/renderingpass/subgraphreplacementregistry.cpp:162-179`) に集約。 | A: logical。B: subgraph-provider ABI。 | |
| `LogicalValueImportKind` (`src/project/logicalrendergraph.hpp:79`)<br>`RenderGraphTransform::ImportKindV1` (`src/core/userpublic/render/graph_transform_abi_v1.hpp:71`) | A の `legacy_implicit` は B の `none` へ写る。B に `legacy_implicit` はなく、A に `none` はない。 | `importKind` (`src/core/renderingpass/graphtransformregistry.cpp:242-260`) に集約。 | A: logical。B: graph-transform ABI。 | |

## 2. 同一概念の別名

| 語 | logical / authoring での実体 | target / physical での実体 | runtime での実体 | 対応の観察 |
|---|---|---|---|---|
| graph | `CompiledLogicalRenderGraph` は resources、imports、logical nodes、compiler decisions を持つ canonical graph (`src/project/logicalrendergraph.hpp:249-261`)。`FrameGraphDefinition` から作る `CompiledLogicalRenderGraph` は shadow/diagnostic-only (`src/core/renderingpass/logicalframegraphadapter.cpp:404-411`)。 | `TargetLoweringGraph` は canonical logical data をコピーした破壊可能 workspace (`src/project/targetrenderplanning.hpp:135-150`)。`VulkanTargetPlan` はその workspace と physical resources/scopes を保持 (`:448-477`)。 | `FrameGraphDefinition` は schedulable config (`src/core/renderingpass/frameplanner.hpp:87-98`)、`FramePlan` は順序・level・barrier (`:120-125`)、`CompiledFrameGraphExecution` は ID/binding 済み実行 (`src/core/renderingpass/framegraphruntime.hpp:41-52`)。 | 同じ `graph` が canonical IR、target workspace、legacy/runtime definition、ordered schedule、bound execution を指す。 |
| pipeline | `RenderPipelineRequest`→`ResolvedRenderPipeline`→`CompiledRenderPipeline` は config/policy/feature の CPU-only compiler chain (`src/project/renderpipeline.hpp:114-187`, `:260-295`)。 | `VulkanTargetPlan` が pipeline 内の graph ごとの physical plan を表す。 | `RenderPipelineRuntimeGeneration` は複数 `CompiledRenderProgram` の一括 publication root (`src/core/renderingpass/framegraphruntime.hpp:54-85`)。`RenderPipelineGpuArena` は同 generation の GPU resource lifetime root (`src/core/renderingpass/renderpipelinegpuarena.hpp:66-82`)。 | logical pipeline は feature/policy metadata、runtime pipeline generation は公開 program 集合、GPU arena は lifetime metadata。 |
| pass | `MaterialPassContract` は material route と attachment/shader ABI の契約 (`src/project/renderpipeline.hpp:43-51`)。`PassDefinition` は render action の config/runtime definition (`src/core/renderingpass/renderingpass.hpp:199-278`)。 | render node は `VulkanPhysicalScopePlan` の `nodes` に入る。scope は複数 node を含み得る (`src/project/targetrenderplanning.hpp:221-240`)。 | `CompiledPass` は `PassId`、graphics view contract、physical rendering contract を持つ (`src/core/renderingpass/renderingpass.hpp:354-359`)。`CompiledRenderingPass` は pass/task の集合 (`:361-365`)。 | `RenderingPassDefinition`→`FrameGraphDefinition` は各 `PassDefinition` を1 render nodeへ写す (`src/core/renderingpass/frameplanner.cpp:1022-1031`)。physical scope と pass は1対1とは限らない。 |
| node | `LogicalGraphNode` は port/use/value-version を持つ canonical work item (`src/project/logicalrendergraph.hpp:162-171`)。 | `TargetLoweringNode` は logical node と dialect/provenance を保持 (`src/project/targetrenderplanning.hpp:118-123`)。scope は1個以上の node name を束ねる。 | `FrameGraphNodeDefinition` は read/write/ordering config、`FramePlanNode` は scheduled copy、`FrameGraphExecutionNode` は runtime array index と incoming barriers、`LogicalFrameNodeInvocation` は physical scope から作る command-recording 1回分 (`src/core/renderingpass/viewexecutionscheduler.hpp:16-45`)。 | `node` は各段で同じ work identity/name を引き継ぐが、definition、scheduled node、executable node、per-view invocation は別レコード。 |
| scope | `SampleCountScope` は sample policy の適用集合 (`geometry` / `all` / `none`; `src/project/samplecountplanning.hpp:21-27`)。 | `VulkanPhysicalScopePlan` は physical execution group (`src/project/targetrenderplanning.hpp:221-240`)。 | `owner_scope` は hot-reload/GPU ownership の文字列 (`src/core/renderingpass/framegraphruntime.hpp:54-62`, `src/core/renderingpass/renderpipelinegpuarena.hpp:54-71`)。`CompiledPassRenderingContract::scope_id` は dynamic-rendering scope (`src/core/renderingpass/renderingpass.hpp:317-347`)。 | policy selection、physical scheduling group、resource owner、dynamic-rendering instance の4用途で `scope` が使われる。 |
| program | `PreviewGraphProgram` は startup-compiled data-only config で、通常の render loop に入らない (`src/core/renderingpass/previewgraph.hpp:14-27`)。 | 該当名なし。 | `CompiledRenderProgram` は `CompiledRenderingPass` と `CompiledFrameGraphExecution` と resource leases の一体 (`src/core/renderingpass/framegraphruntime.hpp:54-62`)。 | `program` は preview では request-local data envelope、runtime では実行・binding・lease 済み publication 単位。 |
| plan | `FramePlan` は frame graph の topological schedule。`ResolvedSampleCountPlan` は resource/group ごとの sample selection。 | `VulkanViewExecutionPlan` は全体 summary、`VulkanNodeViewExecutionPlan` は node 結果、`VulkanPhysical*Plan` は resource/scope/attachment/alias の backend plan、`VulkanTargetPlan` はそれらの root。 | `ViewLayerCopyPlan` は1回の image-array copy range (`src/core/renderingpass/viewexecutionscheduler.hpp:47-99`)。 | `plan` は schedule、policy resolution result、backend object graph、1操作の引数のいずれにも付く。 |
| stage | `TargetLoweringStage` は IR workspace の進行度 (`canonical_workspace` / `target_execution_complete` / `vulkan_physical_complete`; `src/project/targetrenderplanning.hpp:86-92`)。 | 同左。 | shader/pipeline stage とは別であり、この調査範囲の frame node/pass/scope の別名として使う型はない。 | 調査範囲で型名に `Stage` を持つ renderer compiler 型は `TargetLoweringStage`。 |
| request | `GraphVariantPolicyRequest`、`RenderPipelineRequest`、`SampleCountRequest` は authoring/compiler input。 | `TargetBridgeRequest`、`VulkanViewExecutionPlanRequest`、`VulkanSampleCountPlanRequest`、`VulkanExternalDepthExportRequest`、`VulkanTargetPlanRequest` は target/physical compiler input。 | `JsonRpcRequest` (`src/project/jsonrpc.hpp:22-26`) は renderer compiler の request ではない。 | suffix `Request` は入力であることだけを表し、層は前半の語から決まる。 |
| resolved / compiled | `ResolvedRenderPipeline` は JSON を残す resolution result、`CompiledRenderPipeline` は typed immutable runtime contract。`CompiledLogicalRenderGraph` は canonical logical graph。 | `ResolvedSampleCountPlan`、`ResolvedVulkanViewExecutionPlan`、`ResolvedXrMultiviewAutoPolicy` は target/device facts を含む resolution result。 | `CompiledRenderingPass`、`CompiledFrameGraphExecution`、`CompiledRenderProgram` は ID/binding 済み runtime values。 | `Resolved` と `Compiled` は処理段階を表すが、単独では logical / target / physical / runtime を表さない。 |

## 3. same-layer コピー struct とその理由

| struct 群 | 共通部分 / 差分 | コードから読める分離理由 |
|---|---|---|
| `ResolvedRenderPipeline` (`src/project/renderpipeline.hpp:160`)<br>`CompiledRenderPipeline` (`:264`) | 多数の policy/feature fields が共通。前者は `normalized_config` と JSON 形の jitter/features/routing/sort、後者は typed fields と graph-transform/render-strategy selection を持つ。 | typed immutable transition。後者は normalized JSON と GPU/container state を持たない publication value (`:260-263`)。`compileRenderPipeline` は canonical policy を再検査し、JSON fields を typed values に変換してから返す (`src/project/renderpipeline.cpp:1298-1388`)。コメントは runtime container mutation 前に拒否する境界と明記 (`src/project/renderpipeline.hpp:291-295`)。 |
| `FrameGraphNodeDefinition` (`src/core/renderingpass/frameplanner.hpp:66`)<br>`FramePlanNode` (`:100`) | name/kind/declaration index/read/write/snapshot fields をコピー。definition の ordering constraints、attachments、footprints、tags、resolution metadata を plan は持たず、plan は `order` と `level` を加える。 | scheduler output copy。`planFrameGraph` が validation、edge 構築、topological order、level 計算後に必要 fields をコピーする (`src/core/renderingpass/frameplanner.cpp:1098-1137`)。 |
| `VulkanNodeViewExecutionPlan` (`src/project/vulkanviewplanning.hpp:62`)<br>`VulkanPhysicalScopePlan` (`src/project/targetrenderplanning.hpp:221`) | `execution` / `view_count` / `execution_count` / `view_mask` が同一型。node plan は `node` と `reason`、scope plan は scope id/kind、複数 nodes、fusion/local-read/tags/sample count を持つ。 | node resolution を physical scope に集約するコピー。scope 構築時に node plan の4 fields を写し (`src/project/targetrenderplanning.cpp:1544-1669`)、fusion 条件に execution の一致を使う (`:1561-1567`)。 |
| `VulkanPhysicalResourcePlan` / `VulkanPhysicalResourceFragment` (`src/project/targetrenderplanning.hpp:178`, `:345`)<br>`VulkanPhysicalScopePlan` / `VulkanPhysicalScopeFragment` (`:221`, `:354`)<br>`VulkanAliasGroupPlan` / `VulkanPhysicalAliasGroupFragment` (`:285`, `:362`)<br>`VulkanPhysicalAttachmentPlan` / `VulkanPhysicalAttachmentFragment` (`:271`, `:370`) | fragment は同じ physical identity fields の sparse subset。resource は format/representation を optional 化、scope は id/nodes、alias group は全フィールド一致、attachment は aspect を持たず load/store を optional 化。 | 同じ physical layer の sparse edit とヘッダコメントが明記 (`:325-344`)。eject は plan→fragment を一か所で作る (`src/project/vulkanphysicalfragment.cpp:1681-1754`)。link は fragment→plan を検証付きで適用し、alias group の完全コピーは `:2476-2498`。 |
| `VulkanTargetPlanPinPackage` (`src/project/targetrenderplanning.hpp:330`)<br>`VulkanPhysicalFragmentPackage` (`:404`) | `graph`、`logical_graph_fingerprint`、`backend_candidate` が共通。fragment package は schema/edit mode、automatic plan fingerprint と4種の sparse edits を追加。 | pin package は V1 で backend candidate だけを固定する escape hatch (`:325-329`)。fragment package は same-layer physical edits の envelope (`:339-344`, `:404-424`)。 |
| `RenderPipelineGpuScopePreparation` (`src/core/renderingpass/renderpipelinegpuarena.hpp:54`)<br>`RenderPipelineGpuResourceScope` (`:60`) | fields は完全一致: `owner_scope`、`resources`、`resource_leases`。 | failure-atomic publication 境界。前者は GPU registration transaction が作る candidate (`:51-58`)、後者は immutable arena 内の published lifetime root (`:66-82`)。`compileRenderPipelineGpuArena` が preparation を published scope に移す。registration arena は commit がなければ pre-prepare state を復元する (`:115-146`)。 |
| `RenderPipelineProgramPreparation` (`src/core/renderingpass/framegraphruntime.hpp:87`)<br>`CompiledRenderProgram` (`:54`) と `CompiledFrameGraphExecution` (`:41`) | preparation は owner、compiled pass、raw frame plan、pipeline/target plan pointers、name→ID bindings、optional pass ID。published program は確定 pass ID、compiled execution、resource leases を持つ。 | failure-atomic generation candidate。`prepareGeneration` は current generation をコピーして preparation を compile/bind し (`src/core/renderingpass/framegraphruntime.cpp:194-428`)、`publishPreparedGeneration` が generation compare 後に一括 publish する (`:431-456`)。 |
| `FrameGraphResourceContainer::RegistrationCheckpoint` (`src/core/renderingpass/computetask.hpp:63`)<br>`ComputeTaskContainer::RegistrationCheckpoint` (`:134`)<br>`RenderTargetContainer::RegistrationCheckpoint` (`src/core/renderingpass/rendertargetcontainer.hpp:59`) | 3型とも `registration_count` と container 固有 `name_to_id` copy を持つ。compute task 版だけ `next_binding_revision` と `next_task_id` を加える。 | failure-atomic rollback 用の不変コピー。各 container が `checkpointRegistrations` / `rollbackRegistrations` を対で公開 (`src/core/renderingpass/computetask.hpp:83-87`, `:167-170`, `src/core/renderingpass/rendertargetcontainer.hpp:129-134`)。 |
| `LogicalGraphTransformSelection` / `RenderStrategySelection` / `LogicalSubgraphReplacementSelection` (`src/project/logicalrendergraph.hpp:179`, `:199`, `:220`) | `provider`、`implementation`、`contract`、owner/identity/generation/version/capability bits、`explicitly_selected` が共通。各型は transform fingerprints/index、strategy config fingerprints、subgraph region/node lists という別 payload を持つ。 | canonical graph に異なる provider 選択 provenance を保存する形。3型はそれぞれ別 field として `CompiledLogicalRenderGraph` に入り (`:249-260`)、canonical dump で別 object として共通 provenance を出力する (`src/project/logicalrendergraph.cpp:776-870`)。共通 provenance fields を3型に重ねて定義する理由を述べるコメントは読み取れない。 |
| `ResolvedLogicalGraphTransform` (`src/core/renderingpass/graphtransformregistry.hpp:58`)<br>`ResolvedRenderStrategy` (`src/core/renderingpass/renderstrategyregistry.hpp:24`)<br>`ResolvedTaggedRegionReplacement` (`src/core/renderingpass/subgraphreplacementregistry.hpp:47`) | いずれも provider が返した JSON string と domain-specific selection の2フィールド。先頭名は `config_json` / `subgraph_json`。 | 各 registry snapshot の resolve API が domain-specific typed result を返す (`graphtransformregistry.hpp:87-96`, `renderstrategyregistry.hpp:53-62`, `subgraphreplacementregistry.hpp:80-84`)。3つを別 struct にする理由を述べるコメントは読み取れない。 |
| `RenderingSampleCountAssignment` / `RenderingTargetArrayLayerAssignment` / `RenderingTargetFormatAssignment` / `RenderingTargetRepresentationAssignment` (`src/core/renderingpass/renderingsamplecount.hpp:41-74`) | 全型が `resource` と値1個。値だけが samples / array_layers / `vk::Format` / `VulkanResourceRepresentation` で異なる。 | `compileRenderingTargetPlans` が4 vectorを別々に生成・sort (`src/core/renderingpass/renderingsamplecount.cpp:1929-2001`)、`applyRenderingTargetPlan` が4 mapへ別々に検証してから `RenderTargetDefinition` に反映 (`:2100-2165`, `:2210-2289`)。vector を4型に分ける理由を述べるコメントは読み取れない。 |
| `PlanningDiagnostic` / `BackendConstraintFailure` / `PlanningDecision` (`src/project/targetplanning.hpp:151`, `:204`, `:230`) | `id` / `subject` / `detail` が共通。diagnostic は severity、decision は selected を加え、failure は3共通 fields のみ。 | probe result と backend selection が failures/diagnostics/decisions を別 vector で保持 (`:212-244`)。3つを別 struct にする理由を述べるコメントは読み取れない。 |
| `DebugDrawPassInfo` / `DebugTextPassInfo` (`src/core/renderingpass/renderingpass.hpp:116`, `:121`) | fields は完全一致: vertex shader と fragment shader。 | `PassInfo` の別 variant alternative (`:142-147`) で、`PassDefinition` が別 predicate/accessor を持つ (`:238-239`, `:270-273`)。型分離理由を述べるコメントは読み取れない。 |
| `RenderingPassConfigRuntimeDependencies` (`src/core/renderingpass/renderingpassconfigregistration.hpp:47`)<br>`RenderingPassRuntimeDependencies` (`src/core/renderingpass/renderingpassruntimecompiler.hpp:23`) | shader library、fullscreen/shadow/velocity containers、path resolver、defines、warning flag、debug providers が共通。前者は必須 reference、後者は compiler API 用 nullable pointer。後者は metadata/view resolver、frame graph resources、target plan を追加し、pipeline factory を持たない。 | registration-facing bundle から compiler-facing bundle への adapter。`toRuntimeDependencies` (`src/core/renderingpass/renderingpassconfigregistration.cpp:49-93`) の1関数に変換が集約され、debug providers を GPU transaction enlist wrapper で包む。 |

## 4. 名前から層を判別できない型の一覧

この節では、型名に `Logical`、`Target`、`VulkanPhysical`、`Runtime` のような層語があり、その語と実際の所有層が一致するものを「名前から判別できる」とした。`Resolved`、`Compiled`、`Plan`、`Graph`、`Pipeline`、`Pass`、`Program`、`Request` は複数層で使われるため、それだけでは層語として扱っていない。

| 型名 | 定義 | コード上の層・用途 |
|---|---|---|
| `GraphVariantPolicyRequest`, `GraphVariantPolicyCapabilities`, `CompiledGraphVariantPolicy`, `GraphVariantFeatureDecision` と `GraphVariant*` enum 群 | `src/project/graphvariantpolicy.hpp:12-148` | logical graph-program policy。型名に `Logical` はない。 |
| `RenderPipelinePresetInfo`, `RenderPipelinePresetResolution`, `RenderPipelineRequest`, `RenderEnvironmentCapabilities`, `RenderPipelineDiagnostic`, `RenderPipelineResolveDependencies`, `ResolvedRenderPipeline`, `CompiledProjectionJitter`, `CompiledRenderFeatureParameter`, `CompiledRenderFeatureInstance`, `CompiledMaterialRoute`, `CompiledMaterialRouting`, `CompiledDrawSortPolicy`, `CompiledDrawSorting`, `CompiledRenderPipeline` | `src/project/renderpipeline.hpp:87-295` | project/logical CPU compiler の各段階。`RenderPipelineRuntimeGeneration` や `RenderPipelineGpuArena` と stem を共有する。 |
| `SampleCountRequest`, `SampleCountPolicy`, `SampleCountResourceCapability`, `SampleCountGroupRequest`, `ResolvedSampleCountResource`, `ResolvedSampleCountGroup`, `ResolvedSampleCountPlan` | `src/project/samplecountplanning.hpp:29-101` | authoring policy から physical sample selection までを同じ prefix がまたぐ。 |
| `PlanningDiagnostic`, `PlanningDiagnosticPolicy`, `BackendProbeInput`, `BackendConstraintFailure`, `BackendProbeResult`, `PlanningDecision`, `BackendSelection`, `PlanningProfile`, `PlanningNodeConstraint`, `PlanningResourceConstraint`, `PlanningGraphConstraints`, `PlanningNamePair` | `src/project/targetplanning.hpp:142-316` | target compiler の diagnostics/probe/policy。多くは型名に `Target` を含まない。 |
| `ResolvedXrMultiviewAutoPolicy`, `CompiledXrTargetPolicy` | `src/project/xrmultiviewprofile.hpp:67`, `src/project/xrtargetpolicy.hpp:25` | device-dependent target policy と compiled target preference。`Resolved` / `Compiled` だけでは層を区別できない。 |
| `FrameGraphAttachmentDefinition`, `FrameGraphReadFootprintDefinition`, `FrameGraphNodeDefinition`, `FrameGraphDefinition`, `FramePlanNode`, `FramePlanBarrier`, `FramePlan` と対応 enum | `src/core/renderingpass/frameplanner.hpp:13-125` | runtime/config frame graph と runtime schedule。`FrameGraphDefinition` は diagnostic logical shadow の入力にもなるが、logical graph 自体ではない。 |
| `CompiledFrameGraphBarrier`, `FrameGraphExecutionNode`, `CompiledMaterialRouteBinding`, `CompiledFrameGraphExecution`, `CompiledRenderProgram` | `src/core/renderingpass/framegraphruntime.hpp:20-62` | ID/binding 済み runtime execution。`Compiled` と `Program` だけでは層を表さない。 |
| `PreviewGraphProgram` | `src/core/renderingpass/previewgraph.hpp:17` | request-local data-only startup result。`CompiledRenderProgram` と違い通常 runtime render loop に入らない (`:14-16`)。 |
| `MaterialPassInfo`, `FullscreenPassInfo`, `DebugDrawPassInfo`, `DebugTextPassInfo`, `ShadowDepthPassInfo`, `VelocityPassInfo`, `PassImplementationSelection`, `PassInputViewDimension`, `RenderPassViewInvocation`, `PassAttachmentOperations`, `PassDefinition`, `CompiledComputeTask`, `RenderingPassDefinition`, `CompiledPassRenderingContract`, `CompiledPass`, `CompiledRenderingPass` | `src/core/renderingpass/renderingpass.hpp:68-365` | config definitions、physical annotations、runtime compiled values が同じ `Pass` stem の中にある。 |
| `PassImplementationPortContract`, `PassImplementationContract`, `ResolvedFullscreenPassImplementation` | `src/core/renderingpass/passimplementationregistry.hpp:24-55` | logical contract を provider ABI へ渡す compiler/registry boundary と、その選択結果。 |
| `ResolvedRenderStrategy`, `ResolvedRenderStrategyConfig`, `ResolvedTaggedRegionReplacement`, `ResolvedTaggedSubgraphGraph`, `ResolvedTaggedSubgraphConfig` | `src/core/renderingpass/renderstrategyregistry.hpp:24-27`, `:93-104`; `src/core/renderingpass/subgraphreplacementregistry.hpp:47-50`, `:114-128` | logical config/provider resolution。`Resolved` 単独では target resolution result と区別できない。 |
| `RenderingImageFormatCapability`, `RenderingSampleCountAssignment`, `RenderingTargetArrayLayerAssignment`, `RenderingTargetFormatAssignment`, `RenderingTargetRepresentationAssignment`, `RenderingTargetAliasGroupAssignment`, `RenderingTargetPlanDeviceFacts`, `RenderingTargetPlanCompilation` | `src/core/renderingpass/renderingsamplecount.hpp:24-138` | runtime device factsから physical planを作り、runtime targetへ戻す bridge。型名の `RenderingTarget` だけでは target compiler と runtime assignment を区別できない。 |
| `RenderPipelineGpuResourceRegistration`, `RenderPipelineGpuScopePreparation`, `RenderPipelineGpuResourceScope`, `RenderPipelineGpuArena`, `RenderPipelineGpuRegistrationDependencies`, `RenderPipelineGpuRegistryCounts`, `RenderPipelineGpuRegistrationArena` | `src/core/renderingpass/renderpipelinegpuarena.hpp:40-148` | runtime GPU registration transaction、publication lifetime、inspection の3用途。`Gpu` は physical/runtime のどちらかを一意に示さない。 |
| `LogicalFrameNodeInvocation` | `src/core/renderingpass/viewexecutionscheduler.hpp:20-45` | コメント上は physical scope plan から生成する command-recording invocation (`:16-19`)。型名の `Logical` と生成元の physical/runtime 層が一致しない。 |
| `JsonRpcRequest` | `src/project/jsonrpc.hpp:22-26` | renderer compiler の層外にある RPC transport request。renderer の `*Request` 群とは同名 suffix だけが共通。 |

名前から層を判別できる接頭語として実際に一貫している範囲は、canonical graph/type の `Logical*`、target workspace/policy の `Target*`、Vulkan physical records の `VulkanPhysical*`、明示的 publication state の `*Runtime*` である。例外は上表の `LogicalFrameNodeInvocation`。

## 5. 調査範囲

### 5.1 主対象として読んだヘッダ

`src/project/`:

- `src/project/graphvariantpolicy.hpp`
- `src/project/jsonrpc.hpp`
- `src/project/logicalrendergraph.hpp`
- `src/project/materialscreeninput.hpp`
- `src/project/renderpipeline.hpp`
- `src/project/samplecountplanning.hpp`
- `src/project/targetplanning.hpp`
- `src/project/targetrenderplanning.hpp`
- `src/project/vulkanviewplanning.hpp`
- `src/project/xrmultiviewprofile.hpp`
- `src/project/xrtargetpolicy.hpp`

`src/core/renderingpass/`:

- `src/core/renderingpass/computetask.hpp`
- `src/core/renderingpass/framegraphruntime.hpp`
- `src/core/renderingpass/frameplanner.hpp`
- `src/core/renderingpass/graphtransformregistry.hpp`
- `src/core/renderingpass/logicalframegraphadapter.hpp`
- `src/core/renderingpass/passimplementationregistry.hpp`
- `src/core/renderingpass/previewgraph.hpp`
- `src/core/renderingpass/renderingpass.hpp`
- `src/core/renderingpass/renderingpassconfigregistration.hpp`
- `src/core/renderingpass/renderingpasscontainer.hpp`
- `src/core/renderingpass/renderingpassruntimecompiler.hpp`
- `src/core/renderingpass/renderingsamplecount.hpp`
- `src/core/renderingpass/renderpipelinegpuarena.hpp`
- `src/core/renderingpass/renderstrategyregistry.hpp`
- `src/core/renderingpass/rendertargetcontainer.hpp`
- `src/core/renderingpass/rendertargetdefinition.hpp`
- `src/core/renderingpass/rendertargetmetadata.hpp`
- `src/core/renderingpass/subgraphreplacementregistry.hpp`
- `src/core/renderingpass/viewexecutionscheduler.hpp`

### 5.2 型対応の証拠として追加で読んだヘッダ

- `src/core/shader/graphicsviewcontract.hpp`
- `src/core/userpublic/render/graph_transform_abi_v1.hpp`
- `src/core/userpublic/render/pass_implementation_abi_v1.hpp`
- `src/core/userpublic/render/render_strategy_abi_v1.hpp`
- `src/core/userpublic/render/subgraph_replacement_abi_v1.hpp`

変換箇所の確認では、上記ヘッダに対応する `.cpp` と、候補型名を参照する `src/**/*.{hpp,cpp}` を検索した。表中の変換箇所は、その検索で見つかった直接変換の全箇所である。
