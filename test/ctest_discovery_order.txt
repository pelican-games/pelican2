# WP293 compatibility order. Catch2 3.11 randomizes discovery by default;
# reorder_discovered_tests.cmake keeps ctest -N stable across relinks/groups.
Devstudio layout version mismatch selects the default without applying saved state
Devstudio layout names cannot escape their storage directory
Saved devstudio layouts remain restorable as later docks are added
Devstudio layout presets round-trip named versioned state
Devstudio layout restore rejection and invalid data fall back safely
Viewport picks and outliner rows share declaration identity headlessly
Newer outliner selection wins over an older viewport response
Devstudio outliner opens the example without collapsing unnamed objects
Devstudio outliner projects parent references onto declaration identities
Background clears selection while unavailable picking is explicit and preserves it
Devstudio gizmo commit survives save and a fresh authoring reload
Devstudio save scene RPC is serialized only while idle
Devstudio inspector still reflects an external watch change when idle
Devstudio undo uses the editor session revision after a gizmo-style preview
Devstudio numeric editing drives preview update then commit
Devstudio inspector resolves declaration identity before querying components
Devstudio direct edit keeps an in-flight refresh from restoring the old value
Devstudio inspector derives every widget kind from RPC schema
Devstudio watch sync retries when an external revision lands during refresh
Devstudio inspector reports a terminal edit failure instead of accepted as success
Devstudio inspector does not apply an in-flight refresh while a widget is editing
Devstudio aborts the live lease after a terminal preview failure
Devstudio gizmo discards a superseded query failure symmetrically
Devstudio gizmo miss falls back to picking and stale query cannot start a drag
Devstudio gizmo synchronizes selection and toolbar mode over the public RPC
Devstudio gizmo applies the engine projected direction and magnitude
Devstudio gizmo aborts its preview when mode changes during a drag
Devstudio gizmo rotation normalizes quaternion and scale follows DPI
Devstudio gizmo queries once then previews and commits the captured axis
Devstudio frame plan model builds a compact ordered projection of get_frame_plan
Devstudio frame plan model accepts only the current public contract
main loop prepares dependencies before freezing the module graph
runtime reload entry points stay behind ReloadService participants
module construction failures and cycles never publish partial modules
module lifetime scopes cannot overlap
running phase records legal late initialization before enforcement
runtime freeze and shutdown preserve reads but reject new modules
module graph records constructor dependencies and destroys dependents first
RPC module access remains localized to its composition boundary
module creation is owner-thread-only while initialized access remains available
logger falls back to stderr when its file cannot be opened
DeletionQueueCore flushAll releases all pending resources without waiting
DeletionQueueCore releases resources only after the bound GPU submission completes
Deferred callback lease pins its owner until the callback returns
DeletionQueueCore tracks retirement batches by actual submission lease
DeletionQueueCore destructor waits and flushes pending resources as a safety net
DeletionQueueCore teardown drain closes the queue and rejects late resources
Runtime reset drains work without entering terminal shutdown
Exceptional teardown drains every explicit queue in normative order
Allowed module creation permutations finish on normal and exceptional exits
Startup exceptions teardown only the modules and queues already published
GameContext randomInt handles inclusive boundaries
GameContext setSeed reproduces the same random stream
GameContext random sequence matches fixed PCG32 fixture
EngineTime setTime updates the current time without advancing frames
EngineTime setup resets accumulated state
EngineTime advances fixed-step time deterministically
two-bone palette evaluates LINEAR STEP loop and fixed-time determinism
VRM decoder ignores unsupported and absent semantic versions
VRM humanoid rejects duplicate, missing, and invalid node assignments
VRM 1.0 semantic decode retains typed metadata and canonical dump
VRM humanoid optional and unknown bones follow the 1.0 gate
VRM humanoid maps glTF node indices into the A1 rig name layout
VRMA alias and provenance gates reject non-conforming inputs
VRMA-C0 rejects named version mapping and channel violations
VRMA-C0 supports each typed channel inventory independently
VRMA-C0 decodes first animation into body expression and gaze channels
VRMA fixture file round-trips through exact-byte provenance
VRMA-R0 doubles hips translation for a target twice as tall
VRMA-R0 skips only absent optional tracks and target expressions
VRMA-R0 identity rigs preserve body expression and gaze samples
VRMA-R0 composes source rest delta onto target T-pose in world space
VRMA-R0 rejects invalid profile rig and hips scale inputs
interval advance caps reported crossings without moving cursor
interval advance is atomic and handles wrap reverse multi-loop and seek
API descriptor negotiation ignores an unknown caller tail
interval advance bounds hostile repeat traversal
local-to-model and commit history complete one frozen round trip
N-way quaternion reference matches the frozen golden
pose arena is aligned frame-owned and independently usable by two actors
A1 normal blend applies per-joint weights and the frozen N-way quaternion rule
WP99 fixture 6 evaluates and commits two actors in parallel without cross-talk
A1 clamp cursor emits each endpoint interval once without repeating past the clamp
WP99 fixture 1 keeps a rotating non-joint ancestor in the skin palette
WP99 fixture 7 clamp endpoint annotations are never re-emitted
WP147 reload advances only one animation asset generation
A1 jobs build a named real-asset rig and preserve the WP38 palette bytes
WP99 fixture 8 crossing capacity exact one-short and zero preserve cursor atomicity
WP99 fixture 5 invalidates rig clip cursor and pose handles on generation advance
WP99 fixture 3 preserves event order loop indexes and root delta across loops reverse and seek
WP99 fixture 4 N-way blend is byte stable under source permutation
WP99 fixture 2 rejects equal-count poses from a different rig layout
transition decision is force sequence then priority then declaration order
non-finite parameter is rejected and deterministic replay and two actors stay independent
all discontinuity notifications reset graph clocks and require monotonic revisions
WP147 evaluator rebind preserves graph state and unrelated actor frames
clock covers negative speed start offset loop endpoint and blend1d leader tie
zero cut always interrupts and same-tick re-enter resets the state clock
layout mismatch notification survives skeletal asset removal
interrupt materializes one snapshot and alpha zero output is byte identical
pelican.anim_graph v1 loader enforces envelope flat parameters and reserved keys
VRMA-I0 graph blends a generated .vrma twice and commits typed sinks at one revision
VRMA-I0 reload invalidates cursor and pose snapshot then rebinds a new generation
VRMA-I0 graph apply is the sole writer while timeline authority is extract only
old-client new-engine and new-client old-engine negotiate additively
third-party DLL drives clip blend palette and authority commit through public animation headers only
old and new animation header DLLs preserve the frozen prefix
A1.1 service table negotiates without changing the frozen ApiV1 prefix
VRM expression resolution applies override, binary, lookAt and Base plus deltas
VRM bone lookAt maps inner outer up and down to absolute eye rotations
third-party DLL publishes a VRM 1.0 expression frame through the additive public service
bone lookAt stays staged through phase 300 and commits one final palette
VRM expression overrides use one immutable contributor snapshot
ECS prepares worker dependencies on the owner thread before scheduling
Runtime teardown precedes module destruction on normal and exceptional exits
Prepared component swaps follow entities across archetype migration
Free-list LIFO allocation is deterministic across runs
Entity removal invalidates non-forced read systems
Structural callback reentry is rejected before nested state changes
Generation table rejects every stale classification and retires max generation
EntityId canonical representation and value construction are fixed
Modelview GPU instance deinit covers remove clear and teardown
Move-only components relocate without copy fallback
Populate and init failures roll back storage IDs and resources
Bulk creation splits chunks and faults atomically
Aligned non-trivial component observes lifecycle and relocation order
Unified modelview survives JSON population and non-trivial relocation
ECS migration prepare tokens leave live state unchanged and invert publication
ECS entity create and destroy prepared tokens restore id generation and free list
ECS migration commits move-only over-aligned values and lifecycle exactly once
ECS migration construct populate init and adapter faults restore exact live state
Published structural tokens release the core for the next inverse token
ECS migration preflight rejects invalid archetype edits without mutation
ECS migration rolls back and commits system chunk cache and versions
ECS scheduler keeps read read systems parallel
ECS scheduler rejects missing dependencies
ECS runtime strict launch rejects an unordered hazard
ECS runtime applies auto serialization before worker scheduling
ECS scheduler rejects duplicate dependency unexecuted fixtures
ECS strict hazard error names both systems and the component
ECS scheduler rejects dependency cycles and names unexecuted nodes
ECS scheduler accepts hazards ordered by a transitive dependency
ECS auto serialization uses registration order
ECS runtime rejects a dependency removed before update
Registration tokens drive deterministic owner purge
Registration dependencies reject event and component removal
Component registration rejects capacity and duplicate aliases
Registration owner slot reuse rejects a stale generation
Event bus freeze is separate from delivery
Event bus dispatches at frame boundary in emit order
Prevalidator closes every scalar vector quaternion and string JSON shape
Catalog gate documents conditional ref default-branch limitation
Event schema lookup classifies all four registered states and unknown
Event payload descriptor owns and exposes all loader field types
UI binding decisions are complete for every schema lookup state
Catalog gate rejects every descriptor ref drift classification
By-name empty payload matrix is strict for every schema state
Registration and rejected payloads do not construct or load typed events
Global typed event catalog is validated by the mandatory CI gate
BehaviorParams decode applies defaults and present Bool Enum and scalar keys atomically
Struct field type information is separate from all three use-site policies
EventPayload compatibility keeps unknown-before-missing and one loader call per accepted payload
BehaviorParams validation reports stable code and path without partial publication
Structural removal during a behavior callback waits for the next boundary
Behavior pre-destroy runs in reverse attachment order while entity is live
Behavior params canonical roundtrip covers every supported scalar kind
WP167 live behavior edits preserve identity lifecycle and temporal visibility
Every game-logic owner cleanup branch purges registry and live instances
Scene behavior special binder preserves tuple order and separates unknown states
BehaviorSystem literal order and name fix event and update total order
Behavior activation fault rolls initialized attachments back in reverse order
Editor behavior activation failure is terminal until its params change
Behavior registration is declaration-only and params omission applies every default
animation and sprite authored booleans remain JSON booleans after runtime projection
collider trigger is an additive default-false boolean schema field
component codec registry exposes the complete five-tuple
seven component codecs roundtrip authored runtime canonical and fresh load
every component codec rejects invalid type range and unknown key fixtures
transform codec keeps authored local TRS and projects parent-child world TRS
Light and Phys prepared publication is failure atomic
Transform projection recomputes descendants and reparent policies
Structural spawn destroy and mixed command faults restore document and runtime exactly
Editor projection structural stage preserves stable identity and destroy interval
Editor projection restores every adapter prepare and inverse publish fault
Transform projection binds an unnamed child by authoring id
WP172 eval_preview prepare and query faults discard request-local state
WP172 descendant_world remains scoped to the root scene
WP172 PreparedProjection evaluates overrides and explicit staged queries
WP172 eval_preview rejects unsupported prepared fields without publication
WP172 preview graph is a third startup program with an exclusion policy
WP172 typed eval_preview consumes the gate twice and never publishes
WP172 render_preview validates before invocation and captures request-local bytes
WP172 preview insertion leaves the next flat and XR normal capture identical
editor query is deterministic and RPC and ImGui fake adapters are equivalent
ExportSceneSnapshot V1 validates strict request fields and pending state
ImportSceneSnapshot V1 enforces five ordered gates without publication
SAVE0 typed RPC and ImGui surfaces publish the same result
SAVE0 rejects pending work and exposes stable hard error codes
RPC declaration indices follow order after authoring id diverges on insertion
RPC declaration indices match the direct project view for unnamed example objects
ImportSceneSnapshot design example is strict JSON and reaches semantic validation
ExportSceneSnapshot V1 rejects semantic payloads over 64 MiB
ExportSceneSnapshot V1 fixture uses semantic bytes and their real sha256
ImportSceneSnapshot V1 round trips all semantic bytes with fresh identities
WP161 preview open update forced-abort and idempotent abort obey the lease matrix
WP161 reparent descendant cross-edit is an undo conflict
WP161 actor undo and redo are ordinary atomic transactions
WP167 G2 reload overlap is a stable reject at acceptance and execution
WP161 writer history supports multi-level undo and redo on one path
WP157 edit errors use only the canonical catalog
WP157 global CAS rejects an unrelated-object stale edit
WP161 preview commit abort ownership capability and execution gate are stable
WP157 six edit rows enforce acceptance and execution preconditions
WP157 JOURNAL0 is complete and mechanical replay is three-way equivalent
WP157 session ActorId is issued and connection bound
WP161 RPC and windowed paths share one frame-boundary phase trace
WP167 replay and golden gates reject behavior edits before identity allocation
WP167 params edit expands defaults temporarily and undo restores raw omission
WP167 direct behavior attachment edits keep exact handles and sequences
WP161 two-actor structural overlap makes the whole undo a no-op
WP161 behavior attachment sequence survives spawn undo redo
depth pyramid compacts a fixed-state draw segment and preserves CPU fallback
segmented GPU draw generations hot reload and roll back atomically
B-layer directional shadow is visible and copied features are equivalent
GPU-written indexed draw count handles zero max overflow and CPU fallback
golden image cases match expected output
per-view GPU culling preserves segmented draws across XR execution modes
inspector edits unnamed parented runtime objects and changes rendered pixels
depth pyramid compacts multiple CPU-bound material state segments
planar reflection executes a clipped secondary view family on the GPU
multiple directional lights cast matching forward and deferred shadows
set_time automatically resets skinned velocity history
logical frame renders Vulkan-backed stereo views without advancing shared state twice
jitter preserves culling draw count and shadow-map bytes
OpenXR graph transition excludes TAA and restores flat temporal rendering once
velocity feature compiles its standard pass and renders headless
jitter-only Halton and equivalent table captures match byte-exactly
TAA static accumulation is byte-exact across two independent runs
morph-only deformation produces velocity and reset zeros it
GPU timing marks sprite anchor body as supported work
GPU timing reuses one in-flight query ring and caps history at 120 frames
GPU draw timing records a workload sweep without absolute CI thresholds
GPU timing on and off preserve bytes and publish ordered node identity
GPU timing records compute body separately from incoming barriers
fullscreen inputs rebind after shader reload and render-target recreation
glTF loading derives ray-query deformation flags from source geometry
U-USD0b corpus deliveries parse, load, and instantiate their scene fragment
glTF fragments load only the selected object and dependencies
U-USD0c generated delivery parses material and binding then loads the GLB and scene
RGBA8 upload validation rejects metadata and byte-size drift
16-bit glTF PNG is converted before the RGBA8 upload boundary
HR2-G defers model material slot reuse across in-flight frames
HR2-G survives 1000 geometry reloads without allocation growth
WP147 repeated HR2-G reload keeps another model animation running
HR2-G fragment and rig reload contract
HR2-G rig layout change advances compatibility and resets temporal state
WP210d draw queue segments assign disjoint GPU output ranges per fixed state
draw queue compilation is repeatable, input preserving, and fail fast
state_batched_v1 splits large batches without changing stride or offsets
RPE5 bounds use indexed vertices and current deformation envelopes
RPE5 XR queue policy supports one logical center or deterministic per-view order
zero-to-one clip frustum culling is conservative at AABB boundaries
RPE5 phase queues preserve opaque batching and sort transparent back to front
WP206a resolved filters survive flat and two-view queue publication
state_batched_v1 reproduces the legacy command bytes and view ranges
WP206a material tag selection is invariant under registration and sort order
ray-query geometry policy counts BLAS-ineligible static ranges separately
ray-query geometry policy visibly excludes every deformed category
custom draw sort keys are contained and malformed callback output is named
render policy registry rejects malformed descriptors and stale generations
public render policy API binds registrations to the loading owner
builtin draw sort provider is versioned and adds a stable identity tie break
WP200 game owner replaces only implementation data and keeps typed contract
WP200 pass implementation resolution is failure atomic
WP200 pass provider snapshot leases block owner release
WP200 pass provider registry rejects stale handles and public API binds owners
WP200 builtin fullscreen implementation uses the typed provider path
WP201 builtin provider preserves an authored tagged region
WP201 v1 rejects non-contiguous tagged pass regions
WP201 registry lifecycle rejects stale handles and snapshots lease providers
WP201 boundary contract retains an externally materialized terminal output
WP201 invalid replacement is rejected without mutating authored configuration
WP201 custom provider expands a tagged region under the same typed boundary
WP202a registry lifecycle leases provider generations
WP202a rejects invalid candidates without mutating authored config
WP202a graph transforms form an ordered finite chain and survive preset expansion
WP202a builtin graph transform is a typed identity
WP202a provider can expand internal graph structure while preserving its boundary
WP202b strategy can replace the whole renderer seed and observes the XR facade
WP202b render strategy registry leases provider generations
WP202b builtin render strategy preserves authored config through the typed facade
WP202b strategy executes after preset expansion and keeps preset provenance
WP202b rejects malformed strategy candidates without mutating the seed
ModelInstanceId rejects stale and double-remove handles without mutation
Model instance capacity remains an active-live limit and freed slots recover
ModelInstanceId reuses one slot through ten thousand churn cycles
ModelInstanceId retires generation wrap and rejects scene epoch wrap
indirect draw ranges respect the physical-device draw-count limit
SeqPlayer releases playback-owned model instances at scene boundary
ModelInstanceId clear advances scene epoch and invalidates every owner
animation graph key is reserved in component v1
glTF skin and clips connect to animation component without ECS bones
glTF morph targets decode into deterministic shared ranges
skinned morph reload keeps instance identity and resets weights
glTF morph target validation names unsupported input
morph weight frames are per-instance N and N-1 state
VRM firstPerson auto becomes exact node-aware per-view draw ranges
VRM firstPerson auto splits exact head weighted triangles deterministically
WP135 committed VRM decodes renderable geometry
WP135 project graph input and XR activation contracts are safe
WP135 committed VRM is the deterministic full demo fixture
material absolute overrides are source-material scoped and temporal
model reload resets material instance override generation
material instance override validates atomically and reuses storage
model reload resets material absolute override generation
material instance override frames are independent and preserve templates
extracted scene loads through SceneLoader with glTF world transforms
glTF scene extraction is deterministic and maps hierarchy components
non-ray frame descriptor pool retains its exact allocation plan
WP194 rollback and WP195 scope replacement preserve generation-owned GPU resources
material same-pixel resource executes through the tile-local input ABI
WP196 pipeline watcher coalesces dependencies and preserves the active generation on failure
KTX2 cubemap material samples the declared face through Vulkan
project-owned material variant renders a second opaque pass
ray-query acceleration structures remain unbuilt when not requested
ray query and RT pipeline shadow masks match pixel-exactly across the static-only boundary
runtime target planner executes a fused tile-local scope
compute output displaces material vertices through typed resource ports
ray query and RT pipeline shadow masks match for an empty static scene
hybrid_v1 preset registers and renders a headless frame
embedded ray query and RT pipeline shaders render matching real shadows
WP238e NativeScope records Vulkan commands and rebuilds through renderer generations
runtime target planner executes aliased image lifetimes
fixed spatial upscale carries render/output extents and per-input sampling
GPU mip generation rejects missing linear-blit features with a named format
typed image subresources execute a two-stage depth pyramid and rebind after resize
ray-query static BLAS and frame TLAS invalidate on real pool growth and model rebuild
WP218 and WP219 project material writes typed G-buffer targets with independent attachment state
sky ambient feature keeps deferred and forward metals visible with zero lights
dependency-safe physical scopes reorder and fuse real Vulkan rendering
clustered lighting uploads and selects more than 32 lights for every hybrid consumer
headless render target renders and reads back RGBA8 frames
typed Vulkan multiview renders two frame records in one execution
ReloadService keeps queued game logic reloads behind the replay gate
Game system ordering is deterministic by order then registered name
Game logic reload is rejected for deterministic replay drivers
Existing ECS parent transform composes child world as parent times local
Registered game system can read Actions and EngineTime through GameContext
GameContext creates and updates local transforms without exposing modules
lighting inventory v2 retains more than the legacy 32 lights
directional shadow data packs a compact light-major cascade table
light cap warnings name every dropped light and its one-based ordinal
sky ambient runtime values have no engine fallback and require feature parameters
lighting inventory v2 truncates deterministically at byte capacity
light cap warning uses an explicit placeholder for an unnamed dropped light
Camera parses orbit, follow, and fly controller params
Project sprite ppu and default camera policy are validated
Project camera aliases are rejected with v1 replacement names
Camera parses v1 perspective and orthographic projection parameters
Project camera up is a finite non-zero vec3
Camera controller parse errors include camera name and unknown type
Scene camera aliases are rejected with camera and v1 replacement names
Scene camera sprite policy is closed and names invalid values
Runtime navigation camera overlays the resolved scene without changing authored bytes
Project sprite policy objects are closed
constant object translation matches analytic screen-space velocity
projection jitter Halton23 series is one-based and wraps at phase eight
velocity subtracts current and previous jitter in NDC before UV conversion
general projection jitter produces a constant NDC delta for perspective and ortho
projection consumer manifest stays reviewable and anchored to source
render-frame snapshot supplies one jitter offset to material FrameUBO sprite SSAO and debug
temporal reset epoch truth table is invalid for one frame for every reset cause
projection jitter table uses the shared one-based sample index and wrap
planar reflection views preserve identity, clipping, and raster winding
render view family validates stable identities and graph cardinality
view family membership changes invalidate the whole family topology
directional cascade provider creates stable camera-relative family views
zero-to-one oblique projection supports orthographic cameras and safe fallback
temporal view family follows stable ids across execution reordering
runtime view-family providers expose a generic optional package boundary
view family projection jitter is one shared modifier sample
render view family tokens are stable and domain-specific
render view family collection separates main cardinality from secondary families
standard cube capture publishes six stable Vulkan face views
TAA depth linearization reconstructs positive view depth for perspective and ortho
TAA 3x3 neighborhood clamps four sides and corners before blending
TAA reprojection sign OOB edges and corners match the normative UV equations
TAA disocclusion uses a strict greater-than threshold and epoch-pair reset
HDR routes own one tone curve and keep overlays after tonemap
straight alpha, overlap, additive, and bloom operate in linear space
sRGB/data textures and a dual-use image preserve their roles
color known-value, gradient ramp, and fallback parity are analytic
authored sRGB helper decodes RGB but never alpha
glTF COLOR_0 and factors remain raw linear radiometric values
bloom SRGB storage edges stay within one encoded code
RPC load_gltf publishes once and preserves inventory on preflight and GPU failure
gizmo targets an undeclared runtime object with strict identity errors
RPC capture reports contract 2 and absolute encoded-sRGB color
picking feature readback maps a pixel to the WP258 declaration identity
gizmo feature draws and exposes stateless handle queries
image loader reads float EXR as RGBA32F
KTX2 shape errors reject unsupported aggregate dimensions by name
KTX2 parser rejects subset violations with the texture name
image loader reads half EXR as RGBA16F
KTX2 test writer round-trips complete RGBA8 mip levels and sRGB known value
KTX2 loader preserves cube array and volume texture shapes
committed BC7 and BC5 KTX2 fixtures retain compressed blocks
image loader keeps PNG path on RGBA8 stb decode
image loader rejects unsupported EXR pixel type with useful error
InputSnapshot is a serializable value shape
InputStateCore clear returns the snapshot to empty headless state
InputStateCore reports pushed and released edges once per frame
Actions consumption mask does not mutate the public snapshot
InputStateCore stores mouse position and per-frame delta
FrameInput borrow is scoped to one frame generation
InputStateCore derives edges from ordered events within one frame
InputStateCore applies injected axis deltas for one frame
Recorded input sequences use the canonical queue without renumbering
FrameInput carries typed pose samples without changing ordered event sequencing
Gamepad poller is inactive without a bound profile and emits ordered changes
Input events receive a process-lifetime monotonic sequence when queued
OpenXR activation compile-mode-driver matrix
OpenXR window activation uses the injected discovery result
OpenXR discovery conditionally enables Win32 performance-counter conversion
OpenXR discovery conditionally enables composition-layer depth
Vulkan bootstrap requires every feature used by indirect rendering
OpenXR discovery enables the local-floor extension when a 1.0 runtime advertises it
OpenXR discovery reports every pre-session failure stage
OpenXR discovery resolves core and Vulkan commands through the injected table
OpenXR runtime physical-device result overrides the engine heuristic token
Vulkan required extensions merge uniquely and report missing names
synthetic head pose preserves view valid and tracked flags independently
OpenXR diagnostic snapshot drives the additive get_status xr schema
OpenXR session dispatch, lifecycle, and explicit state gates have a stable trace
OpenXR session dispatch resolution and creation failures do not cross into Vulkan
OpenXR terminal events retain distinct paths
OpenXR frame timing is immutable simulation-external data and every frame ends zero-layer
OpenXR QPC margins exist only when the conversion extension is enabled
OpenXR wait, begin, and end frame failures preserve their call boundary
OpenXR event and begin/end session failures are fail-fast with explicit running state
OpenXR actions are created and Touch bindings suggested before the one attach
OpenXR discarded renderable timing still closes with zero layers
OpenXR keeps an available depth swapchain idle for a color-only graph
OpenXR swapchain wait timeout retries the same acquired image
OpenXR shouldRender false closes without swapchain work
OpenXR composition submits a two-layer depth swapchain chain when enabled
OpenXR partial failures unwind each swapchain legally and close zero-layer
OpenXR depth swapchain creation failure falls back to color-only
OpenXR color swapchain creation failure is fatal
OpenXR composition owns one two-layer color swapchain and one projection layer
OpenXR depth failures preserve legal color and depth release order
OpenXR session loss routes to generation teardown without another frame call
OpenXR composition submits both array layers through one view-family command
OpenXR composition retains a submitted generation until both view fences complete
OpenXR identity pose produces identity world/view and zero camera position
flat render view defaults to third-person visibility
OpenXR 64mm IPD keeps eye positions and inverse view translations distinct
OpenXR asymmetric RH ZO projection fixes frustum offsets depth and viewport Y
OpenXR positive 90 degree yaw composes after active world-from-stage
OpenXR reference spaces prefer floor-aware origins and expose LOCAL fallback
OpenXR mirror letterboxes and treats minimized destinations as drops
OpenXR graph policy removes temporal velocity and UI features as units
OpenXR graph policy removes custom velocity and UI providers as units
OpenXR graph validation rejects authored history reads by pass name
OpenXR activation rejects an unknown history feature by name
OpenXR mirror drops transient output maintenance without disabling
pelican.input_seq v1 rejects pose record and replay at start with the action name
pelican.input_seq v1 round trips every WP89 input event kind deterministically
replay reconstructs held and released snapshots through InputState
input_seq rejects non-monotonic event_seq and missing frame markers
deterministic drivers execute zero ImGui callbacks and keep input unchanged
input priority is ImGui then pelican UI then gameplay
interactive graph places ImGui at its canonical anchor
ImGui Vulkan callback rejects every non-success result
plan viewer rejects data outside the public frame plan contract
plan viewer model is an exact deterministic projection of frame plan v1
deterministic drivers execute zero asset browser callbacks and queries
asset browser UI and RPC adapters preserve list_assets results and errors
WP164 UI and RPC adapters preserve query edit undo and preview results
WP164 widget planning is schema-only and covers every inspector field kind
deterministic drivers execute zero inspector callbacks queries and enqueues
WP166 interactive Save uses the typed service and matches RPC
WP179 collider trigger is planned as a schema-derived checkbox
WP164 enum labels are exposed in declaration order with deterministic query bytes
WP245 idle inspector watch still refreshes external scene changes
WP170 stale commit is rejected even when the client never reads watch
WP170 last transaction retains the committing actor attribution
WP245 active inspector edits suppress watch refresh and retain the dragged value
WP167 inspector distinguishes repeated behavior attachments and pending DLL state
WP166 Save is busy for pending tickets and an open preview lease
WP170 watch epoch is monotonic across every preview transition
WP170 repeated watch queries are byte-identical and drive periodic refresh
Compiled plan facts promote the decisions a person checks first
Compiled plan facts tolerate an unknown or partial plan document
Planning opportunities render as readable rows
inject_input params parser reports named invalid params
JSON-RPC response serialization emits success and all required error codes
JSON-RPC parser maps malformed protocol input to required error codes
inject_event params parser reports named invalid params
inject_event params parser accepts type and payload
inject_input params parser accepts key, mouse, and axis events
JSON-RPC request parser accepts valid request objects
windowed RPC dispatches query requests only at a frame boundary
windowed RPC queue overflow returns one stable busy error per rejected line
headless RpcServer blocking loop keeps its exact line protocol bytes
RenderDoc capture reports each completion failure without a second render
RenderDoc armed request rejects an externally changed capture index
RenderDoc capture owns one request and returns the actual new capture
RenderDoc request rejects unavailable XR busy and shutdown boundaries
PathResolver resolves declared asset stores and local overrides
PathResolver project format fixtures
PathResolver rejects invalid asset store declarations and overrides
PathResolver setup is single-use outside tests
PathResolver keeps legacy project paths when no stores are declared
PathResolver existing-file APIs report concrete paths
Standard render algorithm package is a removable engine resource set
Engine resource registry matches project format fixture
PathResolver resolves engine resources before setup
PathResolver rejects symlink escapes after canonicalization
PathResolver parses asset fragments without loading subassets
PathResolver resolves user scheme with user-dir override
shapeCast reports deterministic initial-overlap minimum translation
shapeCast keeps GJK bounded across degenerate convex dimensions
overlap tests include touching boundaries for all shape pairs
raycast hits sphere analytically including tangent and inside rays
shapeCast prevents thin-collider tunneling for every standard shape pair
raycast hits rotated OBBs and rejects misses
raycast hits capsule sides caps and respects max distance
shapeCast rejects non-finite and invalid shape input
raycast rejects degenerate finite query inputs
shapeCastAll shares filters and canonical TOI identity ordering
query filter applies reciprocal layers metadata and stable ignore ids
raycast keeps small sphere and capsule misses stable at long range
raycastClosest resolves equal-distance ties by lexicographic id
raycastAll carries stable identity and derives closest from deterministic order
shapeCast preserves exact box face normals at contact
overlapAll returns deterministic sorted ids
PhysWorld trigger replay emits only symmetric Enter and Exit deltas
Collider component rejects size aliases with v1 replacement names
PhysWorld trigger changes use canonical EntityId pair order
Collider component rejects malformed query metadata and non-finite dimensions
Collider component rejects unknown shapes and negative dimensions
PhysWorld guarantees Exit for destroy and collider removal but not scene reset
Collider component parses sphere box and capsule scene syntax
PhysWorld module follows bound transforms and GameContext exposes queries
Collider component parses query metadata with stable defaults
PhysWorld pure build feeds deterministic raycast and overlap queries
one-way floors pass from below, catch from above, and can be disabled
walkable slopes climb while steep slopes are treated as walls
initial overlap recovery is deterministic and reports ground
fixed-step side-scroller replay and landing events are byte exact
moveAndSlide preserves downward motion while sliding along a wall
moveAndSlide validates the side-scroller plane and callback contract
moveAndSlide catches a high-speed capsule on a thin floor
PhysicsServiceV2 preserves version checks and rejects malformed providers
PhysicsServiceV2 routes shape casts and contains malformed V2 results
PhysicsServiceV2 contains malformed provider results
configured provider shape-casts every standard convex pair
game-owned physics provider activates explicitly and is released by owner
PhysicsServiceV2 negotiates and exposes the configured provider
provider owner release waits for in-flight callbacks
PhysicsServiceV2 rejects malformed descriptors and unknown bits
ProjectBasicConfig loads project.json fixtures
pass definition JSON parser rejects invalid pass object
fullscreen pass JSON parser keeps named resource ports independent from graph edges
render target JSON parser accepts history and its declarative clear
render target JSON parser returns target definitions
material pass info parser applies explicit material range
pass sequence JSON parser rejects duplicate pass names
pass attachment options parser applies explicit color attachment options
rendering pass runtime compiler applies per-attachment physical operations
render target JSON parser keeps mip and array-layer contracts
runtime target format selects typed clear values independently of material schema
material pass contracts parse and filter independently of local pass ids
view-layer copy planning preserves sequential and family image ranges
fullscreen pass JSON parser does not infer options from pass name
generic raster Vulkan adapter maps logical MRT state into physical scope slots
material pass availability distinguishes fullscreen-only graphs
per-view scheduler preserves complete physical scopes
material pass surface resources resolve the feature-owned directional shadow contract
rendering pass runtime compiler pairs definitions with pass ids
shader resource port JSON accepts sampled cube views
rendering pass runtime compiler expands fused attachment mappings
view-family scheduler expands secondary families without repeating them per main view
history clear is encoded after integer target format selection
material pass selects an opaque named variant through an explicit bounded route
typed lighting buffers derive size from render extent and parse compute ports
rendering pass target JSON parser preserves raster attachment subresources
rendering pass JSON helpers parse known values
pass info JSON parser reads shadow depth shader option
rendering pass target JSON parser handles swapchain and omitted input
material output schema rejects duplicate names and lit data in deferred
fullscreen pass JSON parser rejects deprecated projection matrix flag
fullscreen pass JSON parser rejects explicit shader files and names the stem form
view-family scheduler expands mixed physical scopes in scope order
material pass info parser preserves defaults when material range is omitted
compute task schedules distinguish frame and logical-view work
pass definition JSON parser builds an arbitrary-MRT generic raster pass
generic raster pass parser retains open implementation ids and typed resources
view-family scheduler follows dependency-verified physical order
rendering pass target JSON parser applies output and input targets
material pass info parser compiles stable include and exclude tags
rendering pass runtime compiler requires fullscreen dependencies
render target JSON parser rejects malformed format candidates
material output schemas remove the engine MRT count while preserving typed attachment validation
typed GPU draw source creates render dependencies and validates fixed-state command buffers
rendering pass helper ids name invalid and special targets
render target JSON parser accepts fixed extents
shader resource ports type disjoint mip reads and writes
resolver v2 selects B8 SRGB scene/display and float HDR
material pass container resolves one sampled or local-read shader input ABI across variants
pass info JSON parser reads pass type and applies UI defaults
material pass container resolves one output ABI across runtime variants
pass sequence JSON parser allows depth output as later input
rendering pass target JSON parser rejects malformed pass outputs
fullscreen pass JSON parser reads explicit fullscreen options
compute image dispatch derives groups from a typed resource port
fullscreen pass JSON parser accepts explicit read-only buffer ports
rendering pass runtime compiler compiles definition lists
pass definition JSON parser keeps implementation selection typed and fullscreen-only
fullscreen pass JSON parser accepts shader stem references
rendering pass runtime compiler materializes one dynamic-rendering instance for fused scopes
rendering pass JSON helpers reject malformed values
material pass info parser rejects malformed material range
pass attachment options parser preserves defaults when fields are omitted
view-family scheduler rejects incomplete or inconsistent physical contracts
pass attachment options apply sparse per-target clear colors
material output attachment state is sparse typed and schema keyed
ray tracing compute tasks parse supported stages and hard-reject out-of-scope stages
view-family scheduler keeps fused sequential scopes together per view
material pass screen inputs resolve named typed targets and reject mismatches
pass definition JSON parser builds a fullscreen pass definition
pass sequence JSON parser rejects malformed pass list
material pass resource ports resolve named image and buffer reads
typed indirect compute dispatch creates a graph read and validates its command buffer
pass sequence JSON parser validates produced input order
render target JSON parser keeps cube resource shape separate from array layers
rendering pass runtime compiler carries raster subresources into the physical scope contract
fullscreen pass JSON parser keeps per-input sampling typed
pass info JSON parser applies fullscreen info only to fullscreen passes
SceneLoader reports object name for unknown component names
SNAPSHOT0 cache replacement is rollback-safe and never writes the scene file
Scene format rejects legacy lights and names the v1 replacement
ProjectBasicConfig has one authoring document cache authority
Unnamed runtime identities are stable and cannot collide with authored names
Scene format fixtures accept only v1 documents
SAVE0 is failure-atomic at every prepare point and reloads semantically
AuthoringSceneDocument preserves every raw scene object and component
transform_seq sampler floors, clamps, and loops
transform_seq parser rejects malformed files
transform_seq parser reads header, frames, and hidden indexes
pelican.vat parser accepts a valid primitive clip
pelican.vat parser rejects constraint violations clearly
pelican.vat test fixture generates a small GLB
pelican.vat parser leaves non-VAT primitives untouched
shader tool dependencies link and can create compiler/reflection objects
parallel prepare propagates worker failures only after joining
parallel prepare retains declaration order across repeated runs
shader cache key covers defines and transitive source bytes
shader disk cache is byte-identical and recovers from corruption
unwritable shader cache never prevents compilation
shader cache handles long temporary paths and reports fallback once
embedded and runtime shaders share the configured SPIR-V version
UI shader reflection and QuadVertex pipeline layout preserve the 20-byte ABI
rt shadow shader reflects TLAS in frame set and keeps fullscreen input in set one
non-ray frame descriptor layout retains the six-binding ABI
shader compiler compiles GLSL and reports failures without throwing
generated shader resource ports preserve descriptor names and image view dimensions
push constant reflection enforces engine and shader regions
shader stage is inferred from GLSL file extensions
resource port view resolver separates shared consumer and producer-family contracts
shader compiler resolves include directories
shader reflection reports descriptors, push constants, and vertex inputs
comparison texture accessors use shadow sampler signatures
example refraction surface exposes its named screen snapshot accessor
surface compiler emits and reflects an arbitrary material output schema
experimental SPV link compiles B hooks with split descriptor types and stable keys
surface resource ports generate vertex buffer and fragment image accessors
surface physical variant lowers screen and custom image inputs to input attachments
clustered lighting adds typed buffers only to forward surface variants
M3a source composition rejects non-GLSL surfaces by name
surface physical variant compiles a cube material resource accessor
unchanged example toon surface compiles all skinned template variants
surface source composition compiles routed main depth and velocity variants
clustered light selector compiles against the generated buffer interface
standard and toon lighting dogfood only the public surface library
OpenPBR registers and compiles all six routed cache variants
example depth fade lowers same-pixel depth and emits linearization accessor
example material is a one-line B surface reference that lowers and compiles
surface compile diagnostics report the authored snippet line
surface texture dimensions compile to matching GLSL and SPIR-V reflection
directional shadow extends only the forward pass-input ABI and leaves feature-off stable
SPV link backend is opt-in and keeps source composition as the default
SPV link accepts noinline Slang split texture sampler variant corpus
SPIRV-Tools API linker remaps GLSL descriptors and validates warm/cool variants
SPV link corpus covers struct control flow derivative discard and multiple hooks
SPV link ABI rejects arrays matrices and resource handles by symbol name
shader library resolves shader stem project format fixtures
shader library keeps old bundle when reload fails
shader library preserves generated virtual includes across reload
shader references reject explicit extensions and name the stem form
shader library tracks watcher keys and reloads every include and surface variant atomically
shader library loads SPIR-V files and reports dirty reloads
shader library accepts GLSL source files when runtime compiler is enabled
WP218 surface hot reload retains the selected material output ABI
shader library keeps embedded SPIR-V bundles out of reload tracking
VulkanManageCore initializes without a Window in headless mode
ray query bootstrap stays optional and enables its dependency set atomically
VMA buffer device address flag exactly follows logical-device enablement
SBT packing rejects malformed alignment and raygen contracts
SBT packing aligns handle records independently from region bases
AtlasAssetResource grows descriptor pools after the active pool fills
Continuous embedded viewport resizes stay frozen and apply only the trailing extent
Embedded viewport stays horizontally collapsible with visible layout rows
Engine log keeps a bounded UTF-8 tail on line boundaries
Engine process multiplexes JSON-RPC replies without swallowing ordinary output
Abnormal engine process owner exit leaves no child process
Engine process launch rejects a missing executable without starting
Engine process forwards child stdout and stderr through its output signal
Normal engine process owner destruction terminates its child
Device-pixel-ratio changes bypass viewport resize coalescing
Forced engine child exit leaves the viewport process owner reusable
Studio player arguments force the public editor feature overlay
Embedded viewport extent follows Qt device-pixel scaling
process runner captures both streams and saves an append-only log
process API cancellation kills the hanging child without a residual process
CreateProcessW starts an executable from a non-ASCII path
process timeout kills the hanging child without a residual process
import manifest accepts khronos.ktx2 outputs from the ktx2 recipe
import manifest requires version 1 for versioned Pelican outputs
import manifest accepts only pelican.material version 1 outputs
import manifest accepts deterministic K3 deliveries
import manifest fixtures parse and reject expected cases
import rules errors name unknown keys and invalid globs
import globstar spans zero or more complete path segments
import rules evaluate explicit, defaults, builtin, and unmatched precedence
pelican.asset_data v1 is strict and indexes material documents by path
ProjectPathResolver returns diagnostics without an engine logger
pelican.project envelope validation is engine independent
material routing parses the six-state selector without overriding render state
material format parses canonical stable draw tags
minimal material format applies glTF-compatible defaults
six OpenPBR routing states have a byte-stable dump and fixed boundaries
primitive material binding schema rejects duplicate collision fragment and type errors
material format parses optional render path and exact pass separately
full material format round-trips into typed fields and warnings
material texture overrides are limited to declared surface textures
material format fixtures parse and reject expected cases
current material version parses named pass variants without legacy branches
dump includes defines bindings values and render state
six routing variants lower only through lighting hook and fixed forward states
lowering capability errors identify the material and requested feature
named material variants lower through the ordinary surface and route logic
material sampler resolution is deterministic and names fallbacks
screen input materials route forward and reject undefined snapshots by name
glTF core materials converge through the six OpenPBR routing wrappers
material render path overrides are explicit and preserve exact pass intent
type color supports explicit linear encoding without decoding alpha
material lowering carries texture dimension and sampler contract
custom ambient hooks stay on the forward route
surface values use declaration-order std140 offsets
material overrides bind by name without changing declaration layout
OpenPBR base subset routes deferred while extended lobes stay forward
B surface lowering has a byte-stable public C description golden
generic raster contract has no authored attachment-count ceiling
generic raster contract canonicalizes portable draw and attachment state
generic raster fingerprint changes with pipeline-relevant state
generic raster contract rejects malformed typed operations and state
glTF and project material name collision is a named hard error
model template consumes whole-model primitive material bindings
lowered material binder resolves overridden texture references by declared role
binding routing is consumed and project names extend the resolution domain
model template binding errors name missing material mapping and fragment
generic raster nodes participate in one logical planner with typed inputs
frame planner example plan JSON matches fixture
shader resource ports lower sampled and storage intent without creating graph edges
frame planner explicit after and before edges change levels
frame planner preserves existing rendering config order
frame graph labels default and overridden resolution domains
canonical C1b frame-plan diff preserves every legacy pass and target field
frame planner feature-composed config plan matches fixture
frame planner debug text feature plan matches fixture
frame planner accepts compute tasks and serializes node kinds
logical shadow compilation preserves every existing frame plan
snapshot plan node reports copy bytes and screen input read dependency
frame graph parses typed current-read footprints
WP181 frame graph runtime retains one immutable typed pipeline
frame planner emits a barrier for ordered write-after-write
hybrid preset resolves ordered deferred and forward writes
frame planner reports invalid graph fixtures
frame plan preserves a pass-local material variant beside its filter identity
history reads are serialized without an intra-frame dependency
frame plan preserves authored material tag filters with compile provenance
frame planner emits barriers for explicit compute to render resource edges
frame graph preserves raster attachment subresources for physical lowering
frame planner shadow feature plan matches fixture
material resource ports create typed compute to geometry reads
UI feature occupies the canonical pelican_ui anchor and has no base-pass fallback
frame graph preserves per-aspect attachment operations
frame execution validation rejects unclosed endpoint and dependency state
frame execution adapter maps render compute and copy onto one open endpoint vocabulary
frame execution plans canonicalize deterministically without requiring one connected component
generic raster dialect remains visible in the common execution plan
WP193 frame lease retires an old generation only after its last reader
WP193 prepared render generation is invisible until atomic publication
WP195 unchanged foreign programs retain replaced scope dependencies
WP215 window output facts and programs share one renderer publication root
WP193 one snapshot keeps route sample provider and pass revision coherent
WP195 owner replacement keeps old resources for old frames and removes omitted programs
WP193 variant registration appends programs without replacing flat feature publication
WP194 GPU arena candidate and leases stay unpublished until root publication
WP193 stale candidates cannot overwrite a newer publication
WP193 rollback and failed preparation preserve the published generation
WP194 rollback releases unpublished GPU arena leases
logical graph validates port types footprints and relations
legacy frame graph compiles to a diagnostic-only logical shadow
logical type schemas canonicalize defaults and argument order
logical graph keeps nominal connections and fast-default independence explicit
logical value versions derive producer edges without declaration-order semantics
logical conversions are finite deterministic and reject ambiguity
logical shadow adapter models attachment Load as one versioned read-write use
logical type matcher handles exact broad and symbolic constraints
logical shadow adapter preserves declared read footprints
builtin hybrid screen inputs carry typed footprints and explicit display conversions
screen input declaration must match reflected set shape
optimized planning exposes independent work while explicit constraints narrow it
material screen input reflection accepts a physical input attachment
planning warnings stay advisory unless their stable id is strict
compiler provider snapshots retain immutable generation leases
material pass reflection appends the typed directional shadow contract
target topology canonicalizes data-only directed links
finite backend selection is order-independent and records rejected fingerprints
backend probe requires a directed bridge link independently of endpoint facts
hazard stress is seed-reproducible and varies only legal decisions
view execution planning keeps mono and sequential stereo as explicit physical contracts
physical fragment verifier rejects stale, aggressive, open-scope, and overlapping edits
physical fragments select declared alternate formats with target capability evidence
view execution planning supports mixed scopes and rejects an unsatisfied required policy
physical fragments conservatively materialize tile data before splitting scopes
desktop materializes arbitrary G-buffer attachments while tile keeps same-pixel data local
explicit sample target selects its attachment component and blocks incompatible scope fusion
Vulkan target planning preserves cube resource shape independently from view-family layout
secondary-family internal images retain runtime per-view layers
automatic multiview honors measured device profiles while required mode overrides them
complete Vulkan physical verifier rejects stale order, lifetime, and alias metadata
target lowering resolves sample counts on physical resources and scopes
dependency-safe physical scopes preserve explicit before and after dependencies
version 3 physical fragments fuse compatible materialized rendering scopes
attachment budget is a target fact rather than a planner G-buffer constant
NativeScope closes typed effects and keeps unsafe synchronization explicit
neighborhood refraction materializes an opaque snapshot without spilling G-buffer attachments
directional shadow remains one producer-family array for flat preview sequential XR and multiview
Vulkan physical attachment fragments preserve logical dependencies and resolve semantics
alternate physical format evidence is fail-closed for usage and samples
transient candidate elides write-only attachment storage without requiring local reads
complete Vulkan physical plans round-trip and verify as an environment-bound data package
exact target sample planning reports a user-added attachment
Vulkan target plan pins round-trip and bind a logical graph
tile-local fusion preserves materialized attachment operations
target plan fingerprints are deterministic and distinguish structural inputs
target lowering enforces dialect legality and one-way probe capability closure
external depth export remains optional but diagnoses an unsatisfied required request
Vulkan physical fragments round-trip against an automatic target environment
non-overlapping materialized resource lifetimes become legal alias groups
dependency-safe physical scopes reorder independent work and recompute lifetimes
view execution planning selects layered multiview and exposes its Vulkan scope contract
physical attachment plans validate and serialize raster subresources
dependency-safe physical fusion rejects mismatched attachment contracts
external depth export infers camera depth and keeps it materialized across tile lowering
sample-count plan dump is deterministic
exact sample-count rejection identifies the custom attachment
sample-count authoring defaults to exact single sample and rejects ambiguity
sample-count authoring compiles to a typed policy
preferred sample-count fallback names every limiting resource
sample-count planning selects one count for an attachment group
WP192 resolver owns XR and preview policy without injected callbacks
WP192 builtin graph variants compile to explicit execution contracts
WP192 graph feature decisions carry typed exclusion and rejection reasons
WP192 policy compilation rejects unavailable mechanisms without backend lifecycle
WP192 preview transform and validation are selected by typed policy
WP203 pipeline preset settings carry an authored XR target preference
WP206b material variant selection survives every graph variant rewrite
WP203c XR multiview auto profiles resolve measured device evidence
WP203 XR target policy separates authoring preference from the logical graph variant
GPU draw timing observation round-trips a strict typed contract
GPU draw break-even decision requires stable measured gain
RT pipeline shadow mask is a purgeable storage-image compute node
render feature pass binding inheritance rejects invalid dependency graphs
pipeline preset compiles MSAA settings to a typed policy
cube capture feature composes one replaceable six-face hybrid slice
one opaque snapshot becomes a real copy node and sampled render target
hybrid preset keeps the legacy small-light path when clustered lighting is absent
pipeline preset refuses structural deep merge and invalid route contracts
directional shadow contract binds hybrid deferred and forward consumers
scalar feature parameter declaration and instance errors name feature and parameter
projection jitter table schema derives phases and rejects invalid tables
passless render feature records its feature name
shadow directional feature inserts depth pass and lighting dependency
named render-target binding resolves pass IO history and override keys
snapshot v1 rejects a second copy point by name
pipeline preset accepts an explicit typed draw-sort override
gizmo feature owns a purgeable output overlay pass
scalar feature parameters resolve defaults and lower to deterministic defines
surface resource selectors validate even without fullscreen passes
HDR render feature marks scene targets and uses the canonical tonemap anchor
canonical color pipeline rejects unsupported resolver versions
projection jitter rejects a second provider by both feature names
clustered lighting feature shares one typed selection across hybrid consumers
sky ambient feature is purgeable and binds runtime-only values to hybrid
picking feature owns a purgeable integer target and geometry pass
bundled TAA composes into the example pipeline as a two-pass history graph
rt shadow mask is purgeable, compiler-optional, and propagates ray-query planning
shader asset parameters reject type and stage mismatches
render features append buffers and compute tasks
startup feature overlay is explicit, additive, and purgeable
planar reflection feature builds a clipped secondary-family render slice
canonical color pipeline is composed even without features
shader asset feature parameters replace typed shader slots without copying the feature
render features require the runtime shader compiler
snapshot v1 rejects transparent-after copy points by name
startup feature overlay accepts only its strict v1 envelope
named render-target binding errors identify feature parameter and target
hybrid pipeline preset expands to explicit versioned material routes
velocity feature is purgeable and occupies the scene-to-post boundary
projection jitter feature declaration is validated and exposed in compose result
render feature fixtures compose and reject expected cases
WP184 compiles draw sort providers and XR view policy for every graph variant
Vulkan plan pins select the current immutable graph variant
target planning authoring compiles to typed graph-scoped controls
WP184 draw sort declarations reject malformed policy with named errors
Vulkan physical fragments select the current immutable graph variant
WP180 XR resolution preserves policy order suffix and metadata
WP180 resolve failure cannot publish partial registration state
WP181 compile and graph-plan failures precede publication
WP219 material routing carries attachment state across graph variants
WP180 flat resolver preserves FeatureCompose bytes without modules
WP181 compiles dump metadata into a typed immutable runtime contract
target planning authoring rejects ambiguous controls
WP218 material routing carries one output schema across graph variants
WP180 hybrid resolver preserves semantic material routing
WP180 preview precompile resolves presets through the shared boundary
compiler program validation rejects backend and graph-set mismatches
backend-native compiler programs converge at the verified Vulkan package
WP238d provider snapshot and runtime generation retain source code leases through retirement
WP238d builtin NativeScope marker prepares an executable runtime artifact
WP238d NativeScope preparation rejects missing providers and capability drift before publication
WP238d compiler package rejects complete-plan and runtime-plan drift before GPU registration
runtime target adapter selects supported fullscreen local reads and materializes every fallback
scene resolution domain rejects incompatible physical extents
ray query target requirement is hard and uses the enabled runtime capability
runtime target adapter executes supported write-only attachments as transient
runtime target bridge materializes a shared target without losing local-read usage
rendering target bridge applies a verified alternate physical format
color resolve mode follows Vulkan numeric format rules
runtime target adapter carries multiview device facts and typed view requests
runtime target plan carries low-resolution scene and output extent contracts
rendering target bridge applies typed graph planning controls
runtime target bridge preserves cube shape through device capability planning
runtime local-read eligibility accepts material consumers and rejects incompatible extents and multisampling
rendering target bridge links attachment operations into the physical plan
rendering sample planning discovers a hybrid attachment component
runtime depth export stays disabled when the device format cannot be a transfer source
exact rendering sample planning reports an added G-buffer target
XR multiview planning preserves a fused tile-local attachment scope
runtime target bridge applies compatible lifetime alias groups
runtime alias merge drops groups that graph variants do not share
ray tracing pipeline target requirement is hard and uses the enabled runtime capability
explicit target selects its whole attachment component
shader compiler injects feature defines into GLSL compilation
DebugDraw CPU API is a no-op before the feature registers a pass
gizmo projects all three modes and identifies their handles
gizmo grab radius is wider than the raster line and follows DPI
gizmo rejects a camera-facing degenerate axis for dragging
gizmo drag projection follows three camera poses and drawn axes
gizmo translation magnitude grows with camera distance
gizmo rotation uses the positive ring tangent
DebugText CPU API is a no-op before the feature registers a pass
GameContext debugText is a no-op before the feature registers a pass
BitmapFont layout owns DebugText control, fallback, scale, and overflow rules
production DebugText and UI framebuffers are byte-exact
disabled debug utils are a full no-op while node work still runs
debug utils extension selection is optional and deterministic
frame graph labels preserve plan order and nested barriers and body
disabled GPU timing status keeps the additive v2 schema
GPU timing attribution table is an exact fixture
GPU timing identity is the command-label identity plus ordered subrange
unsupported memory budget is a named absence and never a zero-byte heap
memory diagnostics keep driver budgets separate from engine logical accounting
Input profile v1 evaluates wheel axes and modifier button chords
Input profiles switch gamepad buttons and process axes
startup input action overlay is explicit, additive, and rejects named collisions
startup input action overlay accepts only its strict v1 envelope
Repository input profiles remain readable as v1
Input action parser fixtures
Runtime free camera overlays project actions and keeps project profile selection
Input actions evaluate keyboard composites, edges, and set stack consumption
Actions API loads optional input_actions_json through ProjectBasicConfig
Input action backend frames merge without replacing existing device evaluation
GameContext audio reports invalid inputs
Stopped audio voices do not accumulate in either registry
GameContext audio play state works with null backend
GameContext save data is atomic, listed deterministically, and rejects escapes
Persistence settings round-trip uses only user directory and ignores temp residue
Persistence keeps running with named warning state for corrupt settings
assets verification classifies content, structure, case, and strict errors
assets manifest cache rehashes only changed files and full bypasses it
assets manifest generation is byte-idempotent and reuses unchanged hashes
startup assets verification continues normally and fails only in strict mode
surface format preserves ordered declarations and the code split offset
OpenPBR wrapper-B variants preserve one declaration ABI
surface texture declarations preserve dimension and sampler policy
surface format accepts minimal GLSL, HLSL, and Slang headers
surface format declares typed material resource ports
surface texture contract errors name the declaration and valid domain
surface format reflects versioned hooks without changing code identity
surface buffer resource ports require an explicit element
surface format errors identify the source and offending declaration
OpenPBR mapping fixture exact-pins the supported numeric surface
unsupported OpenPBR WARN requires a contributing authored value or connection
Windowed and RPC frames use the same five-phase executor
Editor commit queue hook is optional and has one explicit boundary
central gate adapts legacy shader flag without a second apply path
FileWatcher batch commits each digest only with its transaction result
Win32 watcher covers real filesystem changes overflow rename Unicode and cancellation
ReloadQueue merges an asset and publishes only at a matching frame epoch
Unreadable watcher roots retain live inventory instead of emitting removals
overlapping stores dedupe physical identity and reparse escape is rejected
FileWatcher worker contains scan exceptions and keeps running
FileWatcher local override changes location without changing AssetKey
polling fallback uses fake-clock reconcile, gate cancellation, recovery and degraded state
ContentDigest streams bytes and separates observed live pending and self-write
disabled changes resume through arm-scan barrier exactly once
Transaction runs parse validate stage phases and retires in topological order
ReloadService coalesces one participant's dependency changes into one batch
AssetKey is canonical project identity plus fragment
Frame barrier never exposes half-committed revisions across logical tables
Logical identity survives 1000 commits and generation changes only on slot reuse
ReloadService schedules runtime participants at their declared boundary
ReloadService status preserves HR0 fields and adds resource counters and error
Candidate or stage failure rolls back resource state and reverse edges
ReloadService routes requests through one named participant
ReloadService gives one batch owner selected companion requests atomically
ReloadService rejects ambiguous participant claims before enqueue
GPU submission slots retain a generation until the matching completion
GPU submission slots retire independent in-flight generations
WP215 output compile facts have a deterministic canonical fingerprint
WP216 WSI-only configuration has an independent complete fingerprint
WP215 typed output encoding is stringified only at display boundaries
WP215 every output compiler field participates in the fingerprint
WP215 a frame token retains its renderer generation until consumption
WP215 another target cannot consume a frame token
WP216 a frame token explicitly retains its target epoch
WP215 a dropped frame token abandons exactly its owning frame
WP216 base Vulkan retires only epochs proven by a successor reacquire
WP217 support and swapchain surface loss discard the candidate before one fresh retry
WP217 acquire and present surface loss each allocate exactly one fresh identity
WP216 classifies routine WSI results without exception control flow
WP217 ordered WSI fault scripts consume only the next matching boundary
WP217 WSI fault scripts reject ambiguous rules
WP216 resource pressure is deferred instead of becoming terminal
WP217 fresh surfaces require device rebuild when presentation needs an uncreated queue
WP217 surface loss prepares a distinct surface transaction and retries it
WP217 surface loss requires device rebuild only for an unprovable base present
WP217 swapchain cutover keeps only the legal recovery anchor
WP216 duplicate suboptimal notifications coalesce by recovery key
WP216 zero extent suspends and one positive revision resumes preparation
WP217 fresh surfaces prefer the current presentation queue then a created alternate
WP217 successor reacquire evidence never crosses surface epochs
WP217 call-site policy covers injected WSI result families
WP217 zero extent preserves a pending surface replacement
WP217 a new framebuffer revision bypasses surface retry without changing domains
WP216 failed recovery backs off but a new revision bypasses the old key
HR1-T reload keeps identity, rebinds shape changes, and rolls back failures
HR1-T uses watcher gate and survives 1000 same-shape reloads plus KTX2 drift
WP240c project material names are unique across documents
HR2-S commits surface shader variants and material layout as one transaction
WP206b named variant owns its GPU record and reloads atomically with its base
HR1-M watcher gate and 1000 reloads keep resources bounded
HR1-M updates one same-layout material and rolls back invalid candidates
WP240c project material registry wires texture and values reload
Document emit error fixture covers all EventPayloadSchema lookup failures
Widget arena rejects stale generations and command commit is frame-batched
UI binary64 half-up vectors and viewport edge rounding are normative
pelican.atlas v1 resolves right-exclusive sprites and rejects invalid bounds
U1 QuadVertex ABI, indexed expansion, alternating pages, clips, and limits are normative
U2 bitmap label and button commit local state then queue typed E1 semantics
Document parser validates emit descriptors and stack layout distributes integer remainder
Draw commands are stably sorted and only adjacent equal keys merge
Semantic fixture three-stage gate executes every coverage witness
Ordered router keeps per-pointer captures independent
U2 debug_text legacy layout and common bitmap path rasterize byte-exact
UI runtime and GPU modules remain wholly absent unless requested
U1 nested overflow clips become integer scissor ids
U1 migration rejects the legacy images overlay instead of accepting two formats
asset declarations own sampler and resolve atlas fragments
strict sprite target table is explicit and never silently degrades
sprite total order closes on full EntityId and source-local stable ordinal
sprite draw runs split when uint16 indices restart at a chunk boundary
sprite chunks cross 16384 without loss, duplication, or uint16 overflow
sprite_view schema is closed, finite, ranged, and has no sampler override
50k logical sprites cull to 2k and static revisions control cache reuse
strict pixel contract fixes full viewport math for odd and even zoom 1 2 3
public flipbook deterministically replaces SpriteView texture without engine privilege
AtlasAsset extraction preserves the U1 atlas document byte fields
