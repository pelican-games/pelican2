build*/
dist/
dist_debug/

# Large example assets live next to the JSON scene configs but stay out of git
# (hand-copied between machines; the build copies them beside the player exe)
src/player/resources/*.glb
src/player/resources/*.vrm
src/player/resources/*.png
